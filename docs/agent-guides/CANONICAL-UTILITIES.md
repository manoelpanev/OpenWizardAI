# Canonical Utilities - Full Rationale

The complete, reasoned entry for every commonly-reimplemented function in the codebase. The always-loaded index in [CLAUDE.md](../../CLAUDE.md#commonly-reimplemented-functions-do-not-add-new-copies) lists each entry as one line (name, canonical symbols, path); this file carries the why - the failure each canonical implementation replaced, the invariants it protects, and the traps that made the duplicates subtly wrong. Read the matching entry here BEFORE extending or working around one of these utilities.

Grep-verified 2026-09-04 (`npm run docs:verify` checks that every path below still resolves).

- **ID generation:** `generateId()` in `src/renderer/utils/ids.ts`, `generateUUID()` in `src/shared/uuid.ts`
- **Format file size:** `formatSize()` in `src/shared/formatters.ts`
- **Format numbers:** `formatNumber()` (compact: `1.2M`) and `formatCount()` (exact, grouped: `1,204,993`) in `src/shared/formatters.ts`. Pick by whether the digits ARE the information - a badge wants the magnitude, a filtered row count wants the number.
- **Format tokens:** `formatTokens()`, `formatTokensCompact()`, `estimateTokenCount()` in `src/shared/formatters.ts`
- **Format elapsed time:** `formatElapsedTimeColon()` in `src/shared/formatters.ts`
- **Humanized durations:** everything in `src/shared/duration.ts` (re-exported from `formatters.ts`). `humanizeDuration(ms, opts)` is the one engine; `formatDurationHuman` / `Compact` / `Verbose` / `Parts` / `Decimal` / `Long` / `Words`, plus `formatActiveTime`, `formatElapsedTime`, and `formatTurnDuration` (transcript turn time: day-capped, second-free, `<1m` below a minute), are presets over it. **Never hand-roll another `Math.floor(ms / 86400000)` ladder** - a dozen near-identical copies had already drifted apart on where the ladder stops, whether a zero segment is padded (`"2h 0m"` vs `"2h"`), and whether a countdown rounds up. Those are options (`units`, `maxUnits`, `style`, `keepZeroUnits`, `adjacentUnits`, `round`, `fallback`), so pick a preset or call the engine. Unit sizes are in `DURATION_MS` - don't redeclare `const DAY = 86400000`. See [SHARED-UTILS.md → Durations](docs/agent-guides/SHARED-UTILS.md#durations-srcshareddurationts---both).
- **Format profiling spans (ms):** `formatDuration()` in `src/shared/performance-metrics.ts` (NOT formatters.ts - common mistake). Two-decimal `"123.45ms"` / `"1.23s"` for perf traces only; it caps at seconds, so a multi-day span comes out as a giant seconds float. For anything user-facing use `src/shared/duration.ts`.
- **Format relative time:** `formatRelativeTime()` in `src/shared/formatters.ts`
- **Format cost:** `formatCost()` in `src/shared/formatters.ts`
- **Timestamp for a generated file name:** `fileTimestampSlug(dateOrTimestamp?)` in `src/shared/formatters.ts` - `20260713-142530`, filesystem-safe everywhere and chronologically sortable. LOCAL time on purpose: the name is read by the human who pasted the image at that wall-clock moment, so a UTC stamp reads as the wrong hour to everyone outside UTC. Both the renderer's `saveImageToProject()` and the CLI's `image save` name files with it, so a screenshot saved by hand and the same one saved by an agent cannot come out under two different conventions.
- **Summing a Codex session's tokens:** `CodexTokenCounts` in `src/shared/codexTokenUsage.ts`. Codex reports usage in two shapes and only one is per-turn: `turn.completed.usage` and `info.last_token_usage` are that turn's numbers, while `info.total_token_usage` on a `token_count` event is the running SESSION TOTAL. Summing the total across events is the trap - a session with N turns reports 1+2+...+N times its real tokens, so cost grows with the SQUARE of the conversation length, which is what turned 1.7B input tokens into 75B and let Codex outrank Claude on a dashboard driven almost entirely by Claude agents. The accumulator prefers `last_token_usage`, falls back to a cumulative DELTA, and reads a total that moves backwards as a counter restart (a resume or fork) rather than clamping it to zero. Reasoning output folds into `outputTokens`; `cachedTokens` stays a SUBSET of `inputTokens`. All three transcript scanners go through it so they cannot disagree again - do NOT hand-roll a fourth sum. It has no Electron imports so the CLI can bundle it. **Bump `CODEX_SESSION_CACHE_VERSION` in `src/main/storage/codex-session-storage.ts` whenever this math changes**: entries are reused while `fileMtimeMs` still covers the file, and a parsing fix does not touch a transcript's mtime, so old inflated numbers are served until a session is rewritten.
- **Path utilities:** `truncatePath()`, `getParentDir()`, `truncateCommand()`, `isAbsolutePath()`, `getBasename()` in `src/shared/formatters.ts`
- **Reading a zip from disk:** `readZipArchive(filePath, options?)`, `extractZipTo()`, `isUnsafeZipEntryName()` in `src/main/utils/zip-archive.ts`. Playbook import, Cue backup inspect/restore, and the debug-package tests only need entry names and bytes. The previous reader was `adm-zip`, whose `extractAllTo(..., overwrite)` follows a destination symlink (GHSA-vwc7-r8mq-g2x9) and has no patched release. A wrapper around that API cannot close Dependabot, and `npm audit fix --force` would downgrade to 0.5.8 and re-open the zip-bomb advisory fixed in 0.6.0. `unzipSync` inflates every selected entry, so a caller that only needs `manifest.json` must pass `names` or `filter` (Cue list/inspect do). Default caps on entry count and aggregate `originalSize` refuse a zip bomb before the bytes sit in the main process. `extractZipTo` refuses `..` / absolute names, refuses to overwrite a dest that is already a symlink, and walks every existing path component under the dest root so an intermediate symlink cannot escape. Do NOT add a second unzip helper in a handler or test; `analyze-perf-trace.mjs` is the one exception (it cannot import compiled main-process code) and it still uses `fflate.unzipSync` rather than extract-to-disk.
- **Referencing / thumbnailing a pasted transcript image:** `IMAGE_REF_PREFIX`, `isSessionImageRef()`, `sessionImageThumbnailSrc(ref, maxW, maxH)` in `src/shared/sessionImageRefs.ts`. Pasted images are relocated out of `maestro-sessions.json` into a content-addressed store and referenced as `maestro-image://store/<sha256>.<ext>` (see `src/main/storage/session-image-store.ts`). The prefix used to be a bare `'maestro-image://'` literal repeated across the store, `clipboard.ts`, `imageExport.ts`, and the transcript; it lives here so main and renderer share ONE definition and so the renderer can build a URL without importing fs-dependent main-process code. `sessionImageThumbnailSrc` appends the `?tw=&th=` query that the `maestro-image` protocol handler reads to serve a disk-cached downscaled rendition (`src/main/storage/session-image-thumbnails.ts`). Use it for ANY fixed-size chip: a transcript screenshot is routinely 4984x2578 / 13MB, and Chromium has no way to decode "just the small version" - it decodes the full 12-megapixel bitmap (~48MB RGBA, ~70ms) to paint a 200x80 chip. A field trace of one tab found 47 images totalling 193 megapixels (~0.7GB decoded) thrashing the decode cache on every scroll pass. A BARE ref (no query) must keep serving the original bytes - the lightbox, clipboard copy, and every export path depend on it - so `resolveToFilePath` strips the query rather than treating it as part of the basename, and the traversal guard still runs on the stripped basename. Pair it with `loading="lazy"` + `decoding="async"` on the `<img>`; the thumbnail is what makes the decode cheap, lazy is what stops off-screen work entirely.
- **Loading an image for canvas compositing:** `loadImageElement(src)` in `src/renderer/utils/loadImage.ts`. Wraps `new Image()` + `onload` / `onerror` in a promise and REJECTS on failure, so a caller can never draw a half-decoded element and report success. `compositeAnnotatedImage` and `cropImageDataUrl` both use it, which is what keeps a crop and the save that follows it from disagreeing about whether the source loaded. Accepts anything an `<img src>` accepts, including `maestro-image://` store refs, and the resolved element is decoded so `naturalWidth` / `naturalHeight` are safe to read immediately. Two other loaders in the renderer look like duplicates and are NOT - do not "consolidate" them without reading why: `loadImage()` in `AchievementShareButton.tsx` resolves `null` on purpose (a missing avatar or GitHub logo falls back to a drawn placeholder rather than failing the whole share card), and `svgToPngDataUrl()` in `imageExport.ts` sets `img.decoding = 'async'` before assigning `src`, a hint this helper does not expose. Reach for `loadImageElement` when a failed load must abort the operation.

- **Classifying a file by extension:** `getFileCategory()`, `isPreviewableFile()`, `matchesFileCategory()`, `getFileExtension()` in `src/shared/fileCategories.ts`. One table answers both "can Maestro open this?" and "which bucket is it in?" - `isPreviewableFile` is DERIVED from `getFileCategory`, so a file cannot show up under a filter pill and then refuse to open, nor be openable while invisible under every pill. Audio and video are NOT listed there; they resolve through `getMediaKind()` in `mediaTypes.ts`, which is the single source of truth for what Chromium can decode. Do NOT hand-roll another `TEXT_EXTENSIONS` set - the private copies inside `FileSearchModal` are exactly what this replaced. Distinct from the sets in `src/renderer/utils/fileExplorerIcons/shared.ts`, which exist to pick an ICON and split far more finely (lockfiles, test folders); pick by question and do not merge them.
- **Strip ANSI:** `stripAnsiCodes()` in `src/shared/stringUtils.ts`
- **Reading what a TUI painted, not just its bytes:** `replayTerminalScreen(raw)`, `capturedAlternateScreen(raw)` in `src/maestro-p/screen-replay.ts`. A diffing renderer (Claude Code's alternate-screen UI) rewrites only the cells that changed and jumps the cursor over the rest, so `stripAnsiCodes` on a captured stream loses text that is still on screen: the `/usage` scrape read the second weekly header as a bare `Fable)` and reported that bar as 0%. Replay puts every cell where the terminal drew it. Strip is right for line-oriented output you only need clean; replay is right whenever the capture is cursor-addressed. It deliberately has no wrapping or scrolling, so it needs no PTY size, and it returns the LAST alternate screen even after the TUI switched back out of it.
- **Shell escape:** `shellEscape()`, `shellEscapeArgs()`, `shellEscapeForDoubleQuotes()` in `src/main/utils/shell-escape.ts`
- **Platform detection:** `isWindows()`, `isMacOS()`, `isLinux()` in `src/shared/platformDetection.ts` (works in both processes). In renderer-only code prefer `isWindowsPlatform()`, `isMacOSPlatform()`, `isLinuxPlatform()` in `src/renderer/utils/platformUtils.ts`. Never read `process.platform` in the renderer: the `process` polyfill (`src/renderer/public/process-shim.js`) reports the sentinel `'browser'`, and `navigator.platform` is deprecated and unreliable.
- **Whether a working directory can be spawned into:** `unusableCwdReason(cwd)` in `src/main/process-manager/utils/spawnCwd.ts`, called once by `ProcessManager.spawn()` for both the PTY and child-process paths. A missing `cwd` is an ordinary user state (a deleted worktree, an unmounted volume, a renamed project folder) and it MUST be caught before the spawn, because node-pty's Windows path does not fail anywhere catchable: `pty.spawn()` returns a live-looking object and defers the real work to `WindowsPtyAgent._completePtyConnection()`, which runs later from the conout worker's `onReady` callback (or a 5 s timeout) and calls the native `conptyNative.connect(pty, commandLine, cwd, ...)` there. A `cwd` that is not a directory throws win32 `error 267` (ERROR_DIRECTORY) out of THAT callback - past the `try`/`catch` in `PtySpawner.spawn`, past the IPC handler, into the uncaught-exception handler - while the spawn is reported as successful and the pty is left a zombie with no inner pid, so the caller asks again. Sentry grouped 998 of these crashes under one issue (MAESTRO-S0), 995 of them from a single installation relaunching into the same missing directory. The spawn is REFUSED rather than redirected to the home directory: an agent silently pointed at the wrong tree would read and edit files nobody asked it to. The check is deliberately conservative and only reports a directory it can PROVE is unusable (`ENOENT`/`ENOTDIR`, or a path that stats as a non-directory); any other stat failure returns `null` and lets the spawn proceed, so a locked folder or a flaky network mount can never lock a user out of starting their agent. The refusal emits an `agent-error` naming the directory, so it reaches the user instead of only the log.
- **Modifier-key display text:** `formatKey()`, `formatShortcutKeys()`, `formatMetaKey()` (symbol: `⌘` / `Ctrl`), `formatMetaKeyName()` (prose: `Command` / `Ctrl`), `formatAltKeyName()` (prose: `Option` / `Alt`) in `src/renderer/utils/shortcutFormatter.ts`. Do NOT hard-code `⌘`, `Cmd+`, `Ctrl+`, `Option`, or `Alt` in UI copy, tooltips, or setting descriptions - it renders the wrong key on the other platform. Outside the renderer (CLI, main process) use `formatShortcutKeysFor(keys, isMac)` / `formatKeyFor()` in `src/shared/shortcutKeys.ts`, which owns the key maps; the renderer file is just the platform binding over it.
- **Advertising a shortcut next to the control that fires it:** `ShortcutHint` (`variant="chip"` for an inline key-cap, `variant="row"` for a hint header pinned above a dropdown's option list) and `shortcutSuffix(keys)` (for a `title=` tooltip) in `src/renderer/components/ui/ShortcutHint.tsx`. Maestro is keyboard-first, so every clickable route to an action that ALSO has a chord should show that chord where the user is already looking; this is the one component that draws it. Four byte-identical local `ShortcutHint` definitions had accumulated across the tab-bar components before it was extracted. Two invariants: read the chord from the LIVE shortcut map (`useSettingsStore(s => s.shortcuts)` / `s.tabShortcuts`) rather than spelling a default combo into the string, or a user who rebinds sees our chord instead of theirs; and both helpers render NOTHING for an empty or missing `keys`, because an action can ship unbound and a blank key-cap advertises a combo that does nothing. When a row already has a right-aligned badge, wrap the badge and the key-cap in ONE `ml-auto` container - two auto margins on siblings split the free space and strand the badge mid-row.
- **Whether a chord may be bound at all:** `findReservedShortcutCombo(keys)` over `RESERVED_SHORTCUT_COMBOS` in `src/shared/shortcutKeys.ts`. `Cmd/Ctrl+Shift+Arrow` is the OS's select-to-end inside a text field, and a Maestro binding on one wins in every input in the app, so the user loses the native behavior with no way to tell that apart from a broken text box. It is checked at BOTH ends and the second half is the one that is easy to skip: the recorder (`ShortcutsTab`) refuses a fresh binding, and `migrateShortcuts()` in `settingsStore.ts` strips one already on disk and falls back to the bundled default. A recorder-only guard leaves anyone who already bound the chord broken forever. The table is written in terms of `Meta` and comparison collapses `Ctrl`/`Command` into it, matching `eventMatchesShortcutKeys`, so one entry covers every platform - do NOT add a separate `Ctrl` row.
- **Whether a keystroke belongs to a text field (guard before `preventDefault`):** `isTextInputTarget(target)` and `isTextEntryTarget(target)` in `src/renderer/utils/messageScrollNavigation.ts`; `isEditingTextTarget(target)` / `isEditingTextFocused(doc?)` in `src/renderer/utils/editableTarget.ts`. A surface-level key handler that fires while the caret is in an input steals the keystroke from whatever the user is typing, and the bug reads as the app randomly misbehaving rather than as a shortcut conflict - a bare Enter in the markdown editor also opened the file tree's selected row, because the Files tab held the app's `activeFocus` while the DOM focus was elsewhere. `activeFocus` is APP state and DOM focus is the real thing; when they disagree, real focus wins. Pick by what the key is: **`isTextInputTarget`** counts every `<input>`, `<textarea>`, and contenteditable - use it for arrow keys and Enter, where a range slider legitimately owns Left/Right. **`isTextEntryTarget`** is narrower, counting only inputs that swallow typed characters, so a focused slider or checkbox cannot silently disable `Cmd+Z` or a single-letter tool key. **`isEditingTextTarget`** asks the same question by instance rather than `tagName` (so it sees an `<input>` from another document/realm), and `isEditingTextFocused()` asks it of `document.activeElement` for handlers bound to `window` where the event target is `<body>`. Do NOT hand-roll another `tagName === 'INPUT' || ... || isContentEditable` chain: there are already ~8 inline copies across the renderer (`useKeyboardNavigation`, `DocumentGraphView`, `usePipelineKeyboard`, `MediaViewer`, and others) and they have drifted - some miss contenteditable, `MediaViewer`'s misses `<textarea>` entirely. Import one of these instead; see [DEDUP-TRACKER.md](DEDUP-TRACKER.md).
- **How much of the keyboard the user has mastered:** `collectBoundShortcuts(...maps)` (denominator) and `countUsedBoundShortcuts(bound, usedIds)` (numerator) in `src/renderer/constants/keyboardMastery.ts`. Every mastery figure - the help modal's bar, the Usage Dashboard ring and its Unused Shortcuts list, the leaderboard payload, and the level-up check in `settingsStore` - runs the LIVE maps (`shortcuts`, `tabShortcuts`, `FIXED_SHORTCUTS`) through these, so they cannot disagree about the total. **A shortcut with no chord bound is excluded from both ends**: it can never be fired, so counting one in the denominator makes Keyboard Maestro unreachable forever, and listing one as "unused" tells the user to go press a chord that does not exist. The numerator is not `usedShortcuts.length` either - that list keeps ids whose binding was later cleared, which reports over 100%. Do NOT hand-roll another `Object.keys(DEFAULT_SHORTCUTS).length + ...` total; there were already two rival copies that disagreed with the surfaces rendering the number.
- **Naming the OS file manager in copy:** `getFileManagerName(platform)` / `fileManagerName()` in `src/renderer/utils/platformUtils.ts` return the bare noun (`Finder` / `Explorer` / `File Manager`), and `getRevealLabel()` / `getOpenInLabel()` are built from it. Do NOT hard-code `Finder` in a hint, tooltip, or menu label - it reads wrong on Windows, and the failure is invisible on a Mac dev machine. Same class of bug as hard-coding `⌘`.
- **Color math and contrast:** `readableTextOn()`, `isReadableOn()`, `contrastRatio()`, `relativeLuminance()`, `blendColors()`, `transparentize()`, `adjustBrightness()`, `hexToRgb()` in `src/shared/colorContrast.ts`. Whenever a foreground and its background are BOTH derived from theme colors (diagram fills, chart labels, badges, generated SVG), run the foreground through `readableTextOn(preferred, backgrounds)` so a theme whose accent sits near its text color can't paint near-identical colors on top of each other. It returns the preferred color untouched when it already clears WCAG AA and nudges the theme's own color otherwise, so nothing snaps to hard-coded black/white.
- **Agent display name:** `getAgentDisplayName()` in `src/shared/agentMetadata.ts`
- **Whether an agent is working right now (main process):** `isAgentBusy(session, processManager)` and `isAiTabProcessActive(...)` in `src/main/utils/agent-busy.ts`. **Do NOT read `session.state` or `tab.state` to answer this** - `useDebouncedPersistence` forces every session and tab back to `'idle'` before writing, so a stuck busy state cannot survive a restart and the stored record ALWAYS reads idle. Liveness has to come from what is actually running: a managed process keyed by the compound AI id (`{agentId}-ai-{tabId}`, plus the legacy bare `{agentId}-ai` for the ACTIVE tab only), OR `isSessionBusyWithCli()` for a playbook the CLI drives on that agent. Two levels because two questions get asked - `isAiTabProcessActive` is per-tab (what the web server renders as a tab's state), `isAgentBusy` is per-agent ("may I hand this agent more work?", which Group Chat delegation asks). Distinct from `checkAgentBusy()` in `src/cli/services/agent-busy.ts` despite the matching filename: the CLI is a separate process with no `ProcessManager`, so it reads the sessions JSON off disk. Pick by process, and do not import across the two.
- **Which provider account an agent runs as:** `resolveAgentAccountKey(toolType, env, homeDir)`, `providerProfileKey()` / `parseProviderProfileKey()`, and `providerProfileLabel()` / `providerProfileShortLabel()` in `src/shared/providerProfiles.ts`, with the renderer-side per-agent index in `useProviderProfiles(sessions)` (`src/renderer/hooks/stats/useProviderProfiles.ts`). `toolType` alone stops being the identity the moment a second account is in play: three Claude agents can be three plans with three quota buckets, and only `CLAUDE_CONFIG_DIR` (or `CODEX_HOME`) separates them. `PROVIDER_PROFILE_CONFIGS` is the ONE place that says which providers split by account and what env var does it - the quota panels used to carry their own copy of the pair, which is how a badge's count and a filter's count get to disagree. The effective env is `effectiveAgentCustomEnvVars(sessionEnv, providerEnv)`: the session's own `customEnvVars` REPLACE the agent-level set, they do not layer over it, matching `applyAgentConfigOverrides()` and the CLI's `resolveAgentOverrides()` (a layered merge filed an agent that set only `ANTHROPIC_API_KEY` under a provider-level config dir its process never sees). An API key, gateway, or cloud provider outranks the config dir's login: `resolveAgentBillingCredential(toolType, env)` names it (an API key by its last 4 characters, never the secret), `resolveAgentProfile(toolType, env, homeDir)` files that agent under its own credential profile, and the quota panels do not count it against any plan. An SSH-remote agent's config dir is a path on THAT host holding THAT host's login, so `resolveAgentProfile()` takes the remote and files it under `account @ host` - never the local account of the same directory name, which one real setup had logged into a different account. The quota panels drop remote agents from their counts for the same reason, so a chip's "N agents" and the grid it opens are still the same number. A credential is the same account wherever it is presented, so credential profiles are NOT split by host. `useProviderProfiles().ready` is false until the agent-level env fetch settles; do not clear a profile filter whose key is missing before then, because it may be late rather than gone. Global env vars are deliberately not consulted, since a machine-wide config dir moves every agent at once and is not a per-agent attribution. Resolution returns `null`, and the agent is left unattributed, when $HOME has not resolved and the agent named no dir - guessing the default would file it under an account it may not belong to, and it self-corrects on the next render. `makeAccountKeyHelpers(prefix)` (the `.claude-smash` -> `smash` naming) lives here too and is re-exported from `quota/quotaFormatting.ts` for the panels. Do NOT re-derive an account from an env var at a new call site: the Agents grid's provider filter and the quota tabs' "N agents" chips are the same number by construction, and a click-through from one to the other lands on an empty grid the moment they drift.
- **Keeping a plan account on the Usage Dashboard after its agents leave:** `rememberQuotaAccounts(providerId, keys)`, `getRememberedQuotaAccountKeys()`, `pruneMissingQuotaAccounts()` in `src/main/stores/quotaAccountsStore.ts`, paired with `partitionSnapshotsByAge()` / `SNAPSHOT_RETENTION_MS` in `src/main/stores/usageSnapshotRetention.ts`. The dashboard's account list used to be derived entirely from live state: agents pointing at the account, an on-disk `~/.claude-*` / `~/.codex-*` sweep, and unexpired snapshots. The moment an account hits its limit the user moves every agent off it, so the first source is gone by definition; the sweep misses a symlinked dir, one outside $HOME, and one whose name trips the backup/scratch filter; and the snapshot expires 24h later. The account the user most needs to watch - the one they are waiting on a reset for - was the one that disappeared. Remembering is written by the SAMPLERS (`claude-usage-startup`, `codex-usage-startup`, and the interactive-replay write in `index.ts`), never by a filesystem guess, so a remembered key always means "an agent was configured against this account". The only thing that forgets one is its directory going missing: `pruneMissingQuotaAccounts` forgets on ENOENT/ENOTDIR and KEEPS an unreadable or unmounted dir, since a permissions blip must not erase the row. The retention helper is the other half: a snapshot stops being returned by `getSnapshot()` / `getAllSnapshots()` at the 24h TTL (nothing may DECIDE on a day-old quota reading) but stays on disk for 30 days and is served by `getRetainedSnapshots()` / `getRetainedCodexUsageSnapshots()`, which is what the dashboard IPC mirrors read - so the kept row still shows its last known bars, badged stale by `isSampleExpired()` in `quota/quotaFormatting.ts`. Do NOT widen `getAllSnapshots()` to cover the display case or re-add TTL deletion: the two clocks answer different questions, and collapsing them either lets a stale reading pick a spawn mode or deletes the row again.
- **SSH remote lookup:** `getSshRemoteById()` in `src/main/stores/getters.ts`
- **Deferred main-process store persistence:** `deferStoreWrites(store, memoKey)` in `src/main/stores/deferred-writes.ts`, wired for sessions by `flushPendingSessionWrites()` / `flushPendingSessionWritesSync()` in `src/main/stores/instances.ts`. This is the canonical path for a large, hot `electron-store` document whose synchronous read-parse-stringify-write cycle would block Electron's main thread. It owns the read-through cache, per-array-element serialization memo, coalescing timer, ordered atomic-write drain, recoverable-error retry, and shutdown flush. **Do not wrap another store casually:** caching is safe only when Maestro's main process is the sole writer and no file watcher expects external edits. Mutations must replace memoized array elements immutably; changing one in place deliberately reuses its old serialization. Async commits are revision-ordered, and the final rename is synchronous so `flushSync()` can invalidate an older temp write without that write later replacing the shutdown snapshot. IPC handlers that promise persistence must await `flushPendingSessionWrites()` before acknowledging success; acknowledging the cache update would advance the renderer baseline even if disk later returns `EIO` or `EACCES`. The synchronous form is for shutdown only, where the process is about to be hard-exited.
- **Recovering from a store file that is not JSON:** `createStoreDeserializer(storePath)` and `corruptStorePath(storePath, now?)` in `src/main/stores/corrupt-store-recovery.ts`, wired into every `new Store(...)` by `deserializeStoreJson(name, cwd)` in `src/main/stores/instances.ts`. conf calls the `deserialize` hook from inside its own `store` getter, which runs in the Store CONSTRUCTOR and again on every `get`/`set`, and it rethrows a `SyntaxError` unless `clearInvalidConfig` is set. So a bare `JSON.parse` there means one torn file bricks the app permanently: `initializeStores()` throws before a window exists, relaunching re-reads the same bytes, and nothing on the restart path repairs it. A field crash showed an 8 MB `maestro-sessions.json` truncated mid-write reporting the same unhandled `SyntaxError` hundreds of times in three hours. The recovery moves the file to a stamped `.corrupt-<stamp>.json` sidecar and returns an empty document so conf applies the store's defaults and startup continues. Do NOT reach for conf's `clearInvalidConfig` instead: it discards the document silently, and this file is the only copy of every agent, tab and transcript the user has. Do NOT delete the file or let the next write overwrite it, for the same reason - the stamp exists so a second incident cannot land on the first quarantine. Only a `SyntaxError` is recovered; anything else rethrows, because an unreadable disk is not something to paper over. Atomic writes (`stores/deferred-writes.ts`) remove OUR hand as a cause but not the others: `syncPath` is user-configurable and routinely a cloud-sync folder that truncates and half-materializes files on its own schedule.
- **Toast notifications:** `notifyToast({ color, title, message, dismissible? })` in `src/renderer/stores/notificationStore.ts`. Use for async results, errors, and persistent/dismissable messages. Same five-color design language as Center Flash: `green | yellow | orange | red | theme` (default `theme`). Set `dismissible: true` (or pass `--dismissible` from `maestro-cli notify toast`) when the user MUST acknowledge - disables auto-dismiss, requires click to close, and emphasizes the X button. Cannot combine `dismissible` with `duration`/`--timeout`. External CLI cap: 60 seconds (use `--dismissible` for sticky). **Click actions** (data-driven, survive the IPC bridge): pass `clickAction` for what should happen when the toast body is clicked, or use the legacy `sessionId`/`tabId` fields for plain agent jump. The shape and its validator (`parseToastClickAction`) are canonical in `src/shared/toastClickAction.ts` and aliased by every producer (web-server types, preload, `global.d.ts`, `cue-notify-bridge`); the renderer dispatches every kind through `dispatchToastClickAction()` in `src/renderer/services/toastClickActions.ts`. Kinds: `jump-session` (AI tab), `open-file` (File Preview tab), `open-terminal` (focus an existing terminal tab by id or name; omit `tabRef` for the agent's active one), `open-browser` (focus an in-app browser tab by `tabId`, or open `url` in a new one), `open-url` (system browser). Adding a kind is a change in exactly those two files. From the CLI: `--agent` (+ optional `--tab`), `--open-file <path>`, `--open-terminal [tab]`, `--open-browser <url>`, `--open-browser-tab <id>` (all requiring `--agent`), `--open-url <url>` - mutually exclusive with each other. `--action-url` / `--action-label` render an inline link button beneath the message and are independent of the body click. **Source-agent label:** pass `sourceAgent` (CLI `--source-agent <label>`) to stamp which agent/pipeline fired the toast in the header strip. It's store-independent, so it shows even when the agent isn't loaded in the Left Bar (the name resolved from `--agent`/`sessionId` only renders when that agent is in the desktop store) - use it for cron/watchdog toasts. The explicit label wins over the resolved name for display; pair it with `--agent` to also get click-to-jump. Do NOT pass renderer-only callbacks across the bridge - use `clickAction` instead.
- **Center flash (rapid acks):** `notifyCenterFlash({ message, color, detail?, duration? })` in `src/renderer/stores/centerFlashStore.ts`; clipboard helper `flashCopiedToClipboard()` in `src/renderer/utils/flashCopiedToClipboard.ts`. Use for momentary "I did the thing" confirmations of user-initiated actions. Five-color design language: `green | yellow | orange | red | theme` - default `theme` matches the active Maestro theme. External integrations can fire flashes via `maestro-cli notify flash <message> --color <color>`. Do NOT roll your own center-screen overlay, useState+setTimeout flash, add a sixth color, or use a Toast for clipboard acks. Single visible flash at a time, themed frosted-glass card mounted once in `App.tsx`. Full decision rules, color palette, and design language: [UI-PATTERNS.md → Center Flash System](docs/agent-guides/UI-PATTERNS.md#center-flash-system-rapid-temporary-notifications).
- **Opening a modal / dashboard by name:** `UI_SURFACES` + `resolveUiSurface()` / `resolveUiSurfaceTab()` / `describeSurfaceAccess()` in `src/shared/uiSurfaces.ts`, dispatched by `openUiSurface()` in `src/renderer/utils/openUiSurface.ts`. One registry serves `maestro-cli open <surface> [--tab]`, the main-process `open_modal` validator, and the renderer dispatcher, and it carries the discovery hints (hotkey id, command-palette text, click target) so an agent can teach the manual path while opening the pane. Adding a modal means adding one entry, not touching four files - and if it has tabs, list them there so `--tab` can deep-link.
- **Whether a modal takes the window over:** `DESTINATION_MODALS`, `closeOtherDestinations()`, and `registerExternalDestination()` in `src/renderer/stores/modalStore.ts`. A DESTINATION is a full-window view you GO to (Settings, Usage Dashboard, Director's Notes, Symphony, Cue, Marketplace, Process Monitor, System Logs, Agent Sessions, Memory, plus the Document Graph); only one is ever open, and `openModal`/`toggleModal` evict whichever other one was up. Before this, ordering was decided by each surface's fixed rank in `MODAL_PRIORITIES` instead of by what the user just asked for: the Usage Dashboard (540) opened BEHIND Director's Notes (848), and a main-panel destination opened under any overlay changed nothing on screen - both read as a dead keystroke. A dialog that answers a question ABOUT the surface beneath it is NOT a destination and is meant to layer (confirmations, rename prompts, `cueYamlEditor`, the dashboard's per-agent detail, the Symphony agent picker). Do NOT try to fix a "wrong surface won" bug by re-ranking `MODAL_PRIORITIES` - decide membership here instead. A destination owned by another store (currently only the Document Graph in `fileExplorerStore`) joins via `registerExternalDestination()` and calls `closeOtherDestinations()` on its own way in; `modalStore` must never import that store back. `DESTINATION_SHORTCUT_IDS` in the same file is the second half of that membership question and is DERIVED from it: the window keyboard handler blocks most shortcuts while a modal is up, and consults this set to decide which hotkeys still get through. It used to be a hardcoded chord test (Alt+Cmd plus `l`/`p`/`u`/`s`), which let three destinations through and killed the rest - Director's Notes to Usage Dashboard worked because `Opt+Cmd+U` matched the chord, and the way back did nothing because `Cmd+Shift+O` did not, so switching between two full-window surfaces worked in one direction only and read as a dead key. Deriving the set from `UI_SURFACES` means adding a destination cannot silently drop it back out of the guard, and a user who REBINDS a surface keeps a working hotkey, which a chord test can never promise. Do NOT add a chord back to that guard to fix a "hotkey does nothing over an open surface" bug - give the surface a `shortcutId` in `UI_SURFACES` instead.
- **Toggling the unread filters:** `toggleTabUnreadFilter()`, `toggleAllUnreadFilters()`, and `areUnreadFiltersActive()` in `src/renderer/services/unreadFilters.ts`. The agent filter (`uiStore.showUnreadAgentsOnly`) and the tab filter (`uiStore.showUnreadOnly`) are independent state, but the tab one is NOT a plain setter: entering it saves the active AI tab in `preFilterActiveTabId` and leaving it restores that tab, so writing `setShowUnreadOnly()` directly strands the user on whatever tab the filtered view left them on. The combined toggle counts as active only when BOTH are on, so a half-filtered view COMPLETES rather than clears - "either one" would make one press turn everything off, the opposite of what a half-filtered view asks for. Everything reads the stores at call time rather than a render snapshot, because the palette entry, the keyboard shortcut, and the tab bar button fire from different render trees and a stale snapshot is the one way they disagree about direction. Do NOT inline a fourth copy of the save/restore dance.
- **Scheduled Tasks (clock-driven Cue subs):** types and pure helpers in `src/shared/cue/scheduled-tasks.ts`, filesystem work in `src/main/cue/cue-scheduled-tasks.ts`. `maestro-cli cue schedule` and the Cue modal's Scheduled Tasks tab (via `cue:*ScheduledTask*` IPC) both go through that one module, so they cannot write different YAML for the same task. Do NOT hand-roll another cue.yaml writer.

- **Whether two subscriptions are the same visual trigger:** `triggerGroupKey(sub)` in `src/shared/cue/trigger-group-key.ts`. The pipeline serializer writes fan-out to mixed or command targets as SEVERAL parallel subscriptions that each re-carry the whole trigger config, so "one trigger" is a derived identity, not a stored id. Three surfaces need that identity and must agree: `yamlToPipeline` collapses matching subs onto one trigger node, `cue-engine` fires every sibling when the user clicks Play (so a manual run matches a scheduled tick), and `SessionsTable` picks one representative per group for Run Now. They previously held three hand-mirrored copies, and the two runtime copies had already drifted from the editor's - they omitted `retrigger_on_comments` / `max_notifications`, so Play fired subs a real tick would have treated as separate triggers. Adding a new event-config field means adding it HERE, once: leave it out and two subs that differ only in that field silently collapse into one. Do NOT re-inline a JSON.stringify of the trigger fields at a new call site.
- **How big a pipeline is on the canvas:** `pipelineCardBounds(nodes, opts?)`, plus `nodeFootprintWidth()`, `estimateNodeWidth()` and `NODE_CHROME_OVERHANG` in `src/renderer/components/CuePipelineEditor/utils/nodeFootprint.ts`. Three places need a pipeline's on-screen rect and they must agree: the renderer that sizes the translucent group card (`convertToReactFlowNodes`), the drag handler that stops two dropped pipelines landing on each other (`resolveNonOverlappingPipelineOffset`), and the masonry packer that deals cards into columns (`arrangePipelineGroups`). They used to disagree - only the packer sized a node from its text, while the other two assumed a flat `NODE_BG_WIDTH` - so a node with a long name (a shell step named after the script it runs) rendered outside the card that was supposed to contain it, gear, handle, and "in: <agent>" pill included. A node is bigger than the box ReactFlow positions in two ways: its width is text-driven (`width: max-content`), which `estimateNodeWidth` derives from the same text the node components render and errs WIDE on purpose; and its chrome hangs OUTSIDE that box, since handles are centred on the left and right borders and the badges are pinned past a corner, which `NODE_CHROME_OVERHANG` allows for. Do NOT size a card from `NODE_BG_WIDTH` (it is a FLOOR, not a maximum) and do NOT fold the overhang into `PIPELINE_GROUP_PADDING` - the padding is a visual choice someone may want to tune, and tuning it must not be able to clip a handle.
- **Which pipelines belong to an agent:** `pipelinesForSession(sessionId, pipelines, graphSessions)`, plus `pipelineInvolvesSession()` and `pipelineNamesDeclaredBySession()` in `src/renderer/components/CuePipelineEditor/utils/pipelineMembership.ts`. "Belongs" has two independent senses and the Cue dashboard needs both: PARTICIPATION (an agent node bound to the session, or a command node whose `owningSessionId` is the session) and DECLARATION (the pipeline is defined in that agent's own `cue.yaml`, even when no node points back at it - a fan-out that only dispatches to other agents, or an `action: command` pipeline that is just a trigger plus a shell step). The predecessor, `getPipelineColorForAgent()`, answered a COLOR question and checked agent nodes only, which broke twice over: a command-only pipeline belonged to nobody, so the Sessions table printed a dash and "View in Graph" dropped the user into every pipeline on the machine; and because two pipelines can legitimately share a color, pairing a color list back to names dropped one and mislabeled the other. Membership is resolved by pipeline identity, never by color - `PipelineDot` is keyed on `p.id` for the same reason. Declaration matches on `getPipelineKey(sub)` because names are the grouping key `subscriptionsToPipelines` builds with, so they equal `CuePipeline.name` exactly. Do NOT reintroduce a color-based lookup, and do NOT filter agent nodes inline at a new call site.
- **How much work happened in a group chat:** `computeGroupChatActivity(entries)` in `src/shared/groupChatActivity.ts`, fed by the per-turn measurements `src/main/group-chat/group-chat-turn-metrics.ts` stamps onto each history entry. **Never present the span between a chat's first and last message as effort** - a group chat is a ROOM, not a task, so a chat used twice a week reports hundreds of hours without anyone doing anything (a real one read `481h 34m` for about five hours of work). Working time is the UNION of the busy intervals, because participants run in PARALLEL: three agents working the same ten minutes is ten minutes of chat time, not thirty, and summing per-agent durations produces a number larger than the chat's own age. Entries written before per-turn timing existed carry no `elapsedTimeMs` and degenerate to points, so `GROUP_CHAT_ACTIVITY_STITCH_MS` treats turns closer than five minutes as one block - without it an older chat reports `0m`, which is a worse lie than the span it replaced. Turn duration is measured with `beginSleepAwareSpan()`, never a bare `Date.now()` delta. `turnsWithTokens` / `turnsWithCost` are coverage counts: render a dash rather than a zero when they are 0, since a chat whose turns reported no usage is UNKNOWN, not free. Both the Group Chat Info overlay and the HTML export call the one function, so they cannot disagree about how much work a chat did.
- **How big a tab's conversation is and how long it ran:** `computeTabConversationStats(logs)` and `formatConversationDuration(ms)` in `src/shared/tabConversationStats.ts`. Three surfaces read the figures from here - the Context Details popover in the main panel header (`MainPanelHeader.tsx`), the HTML export (`generateTabExportHtml` in `src/renderer/utils/tabExport.ts`), and `maestro-cli sessions` (`src/cli/commands/list-sessions.ts`) - so what the app shows live, what a saved transcript prints, and what the CLI reports cannot drift. The CLI's own count used to filter on `stdin`, which is not a `LogEntry['source']` at all, so it silently reported the `stdout` count and nothing else. **The CLI has TWO readers and both report these figures.** Non-Claude agents are counted straight from the stored tab. The Claude reader derives its numbers by scanning `~/.claude/projects/*.jsonl`, because that file is the only record of a session Maestro never rendered (one started in a bare terminal, or predating the install), so it cannot simply be replaced - instead `overlayTabConversationStats()` swaps in the tab's figures for every session a tab exists for, which is exactly the set the popover can show. A tab with an EMPTY log is deliberately left out of that index: overlaying it would report `0 msgs` for a real conversation whose transcript is only on disk. The overlay runs after `listClaudeSessions` has sorted and paginated, which is safe only because it sorts on `modifiedAt` and searches `sessionName` / `firstMessage` - neither figure is a sort or filter key, so a new sort option over `messageCount` would have to move the overlay earlier. It lives in `shared/` rather than `renderer/utils/` for that third caller: the CLI tsconfig has no DOM lib, so importing the renderer type barrel from there fails on modules that reference `window`. Its input type is structural (`ConversationLogEntry`, just `timestamp` and `source`) for the same reason, and the generic hands the caller's own entries back unchanged. **`totalMessages` counts EVERY conversation entry**, tool calls and thinking included, because the export draws one card per entry and its Messages card has always meant "cards in this document" - the user/agent split is returned separately (`userMessages` / `aiMessages`) for surfaces that want to break it down, which is what the popover puts in the row's hover title. The source list is `CONVERSATION_LOG_SOURCES`, so a new transcript log source is admitted once rather than in each surface's own literal array. **The duration is a WALL-CLOCK SPAN** between the first and last entry, not working time: a tab left open overnight reports the night. For ONE tab that is the honest reading of "how long has this been going", but do not present it as effort, and do not copy the pattern to a group chat - a room's span is meaningless, which is why `computeGroupChatActivity` exists instead. Below two entries the span is zero, and `formatConversationDuration` renders that as `0m` rather than the `0s` a bare `formatDurationCompact` would give, matching what the export has always printed. Walking the log array is O(entries), so the popover computes it only while it is actually open.
- **Whether an agent is drawn in the Left Bar:** `sessionMatchesFilter()` and `passesUnreadFilter()` in `src/renderer/utils/sidebarMembership.ts`. "Visible" means drawn ANYWHERE in the scrollable list; scroll position is irrelevant. Both the render path (`useSessionCategories`) and `Cmd+[` / `Cmd+]` (`useCycleSession`) call these, because a cycle that disagrees with the list about membership moves the cursor onto an agent that is not on screen, which reads as the shortcut jumping at random. Adding a match rule here fixes both at once. The "does this agent need attention?" half of that answer lives in `sessionNeedsAttention()` / `sessionOrChildrenNeedAttention()` in `src/renderer/utils/sessionAttention.ts`, because the bell badge, the collapsed rail (`SkinnySidebar`), and the jump-badge projection (`useSortedSessions`) ask it WITHOUT a filter context - so a new attention rule (unread, busy, **error**, Auto Run batch, stuck outage) goes there and every surface picks it up at once. Errored agents were invisible in the unread filter for exactly this reason: four rival copies of the predicate, and only one of them knew about `state === 'error'`. The inputs are shared state (`uiStore.sessionFilter`, `uiStore.showArchivedGroupChats`) rather than local `useState` for the same reason - see [STATE-PATTERNS.md → uiStore](docs/agent-guides/STATE-PATTERNS.md#uistore). Note that `orderGroupChatsForDisplay()` drops archived chats ITSELF, so a caller that has already decided archived chats are on screen must pass `{ includeArchived: true }` rather than pre-filtering, or they are removed again on the way through.
- **Session lookup:** `selectActiveSession()`, `selectSessionById()` in `src/renderer/stores/sessionStore.ts`; `useActiveSession()` hook in `src/renderer/hooks/session/useActiveSession.ts`
- **Session mutation:** `updateSessionWith(sessionId, updater)` in `src/renderer/stores/sessionStore.ts` (do NOT hand-roll `setSessions(prev => prev.map(...))`)
- **Moving an agent to another directory:** `withWorkingDirectory(session, dir)`, `workingDirectoryChangeBlocker(session)`, `isSameDirectory(a, b)`, `rebasePathOntoRoot()` in `src/renderer/utils/agentWorkingDirectory.ts`. An agent's location is spread across `cwd`, `fullPath`, `shellCwd`, `projectRoot`, `autoRunFolderPath`, and an SSH agent's `workingDirOverride`. `update-agent --cwd` once moved only the first three, so the Files panel and the Edit dialog stayed on the old folder while the agent ran in the new one (#1565). Every relocation goes through `withWorkingDirectory()`, which moves them together, rebases an Auto Run folder only when it lives under the old root, and clears the file tree and git state so both reload. Its "already there" check compares `cwd`, `fullPath`, and the SSH override as well as `projectRoot`, so moving a split agent onto its own `projectRoot` repairs it; `shellCwd` is left out because a terminal `cd` moves it on purpose. Windows paths compare case-insensitively; compare a user-entered directory with `isSameDirectory()`, never `===`, or a trailing slash reads as a move. It also puts down an in-flight file tree load and forgets an SSH agent's reported `remoteCwd`, both of which otherwise keep describing the old project; `useFileTreeManagement` refuses to write a scan whose root no longer matches the session, so a scan that finishes mid-move cannot land either. Gate the move on `workingDirectoryChangeBlocker()`, not `aiPid` alone: a spawned process keeps the cwd it launched with, and a busy agent can have no recorded pid. `useCueAutoDiscovery` refreshes the Cue engine when `projectRoot` changes, so do not add a second refresh at the call site.
- **Per-agent git actions:** `useGitAgentActions(session)` in `src/renderer/hooks/git/useGitAgentActions.ts` - the shared View Git Log / View Git Diff / Pull / Push / Change Branch / Create PR / Configure Worktrees set behind ALL THREE surfaces that offer it: the header branch pill dropdown, the Left Bar right-click menu, and the command palette (`buildGitWorktreeCommands`). Also exports `resolveGitCwd()` / `resolveGitSshRemoteId()`. Adding a git action means extending this hook and surfacing it in all three, not re-deriving cwd + SSH id + modal calls. See [REMAINING-SYSTEMS.md](docs/agent-guides/REMAINING-SYSTEMS.md#usegitagentactions-srcrendererhooksgitusegitagentactionsts).
- **Whether a PR is being opened right now:** `usePRCreationActive(worktreePath)` in `src/renderer/stores/prCreationStore.ts` - `gh pr create` can take a minute, so the request lives in this store rather than in `CreatePRModal`, which the user is free to close while it works. `startPRCreation(target)` fires it (a second call on the same repo re-attaches instead of opening a duplicate PR), `usePRCreationActive` is what the menus badge with, and `usePRCreationNotifier` reports the settlement exactly once - toast plus history entry - from a host that stays mounted. Do NOT `await window.maestro.git.createPR` inside a component: a close would throw away the PR's url and its error with it. Same shape as `gitCommandRunStore` for pull/push.
- **Taking the user to an agent:** `jumpToAgent(sessionId, { tabId? })`, `revealAgentInSidebar(session)`, `openAgentSettings(session)` in `src/renderer/services/agentNavigation.ts`. Landing on an agent is never just `setActiveSessionId`: the Document Graph is a full-screen overlay that would sit on top of it, an active group chat keeps rendering instead of the transcript, the agent may have been left on a terminal / file / browser tab (all of which outrank the AI tab in the render precedence), and the agent itself may be inside a collapsed group or a collapsed bookmarks section. Every caller that forgot one of those steps produced a jump that looked to the user like the click did nothing, which is why the reveal half was lifted out of `QuickActionsModal` into this module. `jumpToAgent` returns `false` when the agent no longer exists so the caller can SAY so - a silent no-op on a deleted agent is the same failure in a new shape. Everything self-sources from the stores rather than props, so a modal deep in a tree can call it without threading `setActiveSessionId` / `setGroups` / `setBookmarksCollapsed` callbacks down; that also means a test drives it through `useSessionStore.setState`, not prop spies. The reveal is deliberately MINIMAL: a bookmarked agent that is already visible in an expanded group leaves both sections exactly as the user left them, and when neither is open it expands bookmarks only, the lighter of the two reveals. `openAgentSettings` is the Edit Agent modal, kept here so a surface offering "jump" and "configure" side by side gets both from one import. A caller that sits UNDER a full-window modal (the Usage Dashboard) must close that modal itself after the jump, or the agent lands behind it.
- **Focus an AI tab:** `aiTabFocusFields(tabId?)` in `src/renderer/utils/tabHelpers.ts` - spread into a session patch (`{ ...s, ...aiTabFocusFields(tabId) }`) to land on an AI tab. It clears `activeFileTabId`/`activeTerminalTabId`/`activeBrowserTabId` and sets `inputMode: 'ai'`. Do NOT hand-roll the literal: those non-AI tab types outrank the AI tab in the render precedence, so omitting even one leaves the previous view on screen.
- **Focus a file tab:** `fileTabFocusFields(tabId)` in `src/renderer/utils/tabHelpers.ts` - the file-tab counterpart to the above. Clears the terminal and browser selections (both outrank the file tab) and sets `inputMode: 'ai'`.
- **Closing a tab while the unread filter is on:** `closeTab(session, tabId, showUnreadOnly?, options?)` in `src/renderer/utils/tabHelpers.ts`. The third argument is an OVERRIDE, not the filter state: omit it and `closeTab` reads `uiStore.showUnreadOnly` itself. It used to default to `false`, and five of the six call sites took that default, so closing a tab under the filter ran the plain left-neighbor math and activated a tab the filter was hiding - the user pressed Cmd+W and landed somewhere not on screen. The replacement is now picked from `filterUnifiedTabOrderForUnread(...)`, the exact list `TabBar` renders, computed against the PRE-close session so the closed tab still occupies its slot; the left neighbor of that slot wins, which means a terminal/file/browser tab that survives the filter is a legitimate target and the result is a `UnifiedTabRef`, not an AI tab id. Pass an explicit `false` only where no tab survives to be selected (bulk close in `unifiedCloseHelpers.ts`, the close-agent path in `useAITabHandlers.ts`) or in a test that pins the state; anywhere else, omit it. `snoozeTab()` in `src/renderer/utils/snoozeHelpers.ts` and `tabStore.snoozeTab()` thread the same optional override through for the same reason.
- **Ending a turn with no process exit:** `settleTabThinkingState(session, tabId)` in `src/renderer/utils/tabHelpers.ts` - the inverse of `markTabRunningQueuedItem`. A tab's pulsing dot and its row on the Thinking pill are driven by `tab.state === 'busy'` plus the session's `state`/`busySource`/`thinkingStartTime`, and normally ONLY the process-exit listener clears them. Any path that ends a turn without a process exiting has to settle that state itself, or the tab pulses forever and the pill keeps counting elapsed time for work nobody is doing - `cancelRetry` (`src/renderer/stores/retryStore.ts`) is the live case, where a `scheduled` auto-retry's resend never spawned. Do NOT hand-roll the patch: it also recomputes the SESSION from whatever is still running (another busy tab, or an entry in `orphanedThinkingTabs` - a tab the user closed mid-turn, kept solely so the pill keeps counting it, which settling retires outright), and it deliberately leaves `state === 'error'` alone, since that is the blocking modal's state and settling a tab must not dismiss an error the user still has to act on. Only settle a tab with nothing behind it: an `in-flight` resend IS a live process, and its busy state belongs to the exit listener.
- **Leaving inline wizard mode:** `flattenWizardIntoTab(tab, { summary? })` in `src/renderer/utils/tabHelpers.ts`. The inline wizard (`/wizard`) keeps its conversation in `tab.wizardState.conversationHistory` and renders it through `WizardConversationView`, a store entirely separate from `tab.logs` / `TerminalOutput`. So dropping `wizardState` drops the whole wizard conversation, and the provider session handle with it, leaving a tab that looks empty - which is what shipped for every exit except a successful completion. Do NOT set `wizardState: undefined` by hand anywhere; call this instead. It appends the transcript to `tab.logs` (user turns as `source: 'user'`, agent turns as `'ai'`), promotes `wizardState.agentSessionId` onto `tab.agentSessionId` so the plain tab can keep talking to the same provider context, appends an optional closing `summary` entry, and only then clears the wizard. It is idempotent (entries are matched by their `wizard-`prefixed ids), which matters because the wizard sync effect in `useWizardHandlers.ts` can race a caller that already flattened. Three callers, one per exit: `completeWizardImpl` (with the "Wizard Complete" summary), `handleExitWizard` (the Exit Wizard button and the cancel control on the document generation view), and the restart sweep plus the sync effect's safety net. Deliberately NOT wired to closing the wizard TAB: that already warns the user and passes `skipHistory`, and the tab itself is going away. See [[CLAUDE-WIZARD.md]].
- **Naming a tab from what the user asked for:** `requestTabAutoName({ session, tabId, prompt })` in `src/renderer/services/tabAutoNaming.ts`, with `collectNamingPrompt()` to build the capped prompt out of the user's own messages and `requestWizardTabAutoName()` for the wizard's variant. Naming is four steps - build the prompt, try the free client-side pattern match (`extractQuickTabName`, which turns a PR/issue/ticket reference into a name with no spawn), spawn the ephemeral namer with the SAME provider, cwd, env and SSH remote the chat uses, then write the result back ONLY if the tab has not been renamed in the meantime. Three surfaces wanted all four and each hand-rolled them: the send path (`useInputProcessing`), the rename modal's "Auto" button (`useSessionLifecycle`), and the inline wizard. The copies had already drifted - only the send path re-checked the tab before writing, so a slow naming turn from the rename modal could stomp a name the user typed while it ran. Pass the `session` rather than an id: the namer must inherit the sending agent's provider auth and Claude token source (`getClaudeTokenSourceFields`), and looking it up separately is how those drift apart. `canApply` is checked twice, once before the spawn and once against the LIVE tab before the write, and defaults to "only name a tab with no name"; `force: true` is for the rename modal, where the user asked explicitly and the `automaticTabNamingEnabled` setting and the existing name are both beside the point. **A wizard tab is "unnamed" while it still reads `Wizard`.** It is created before anyone knows the subject, so `WIZARD_TAB_PLACEHOLDER_NAME` is the placeholder and `isWizardTabAutoNameable()` - not `!tab.name` - is the question to ask; the name that lands carries `WIZARD_TAB_NAME_PREFIX` (`wizard: Ingest Pipeline`) so the tab still reads as a wizard at a glance. It is triggered from the `/wizard <input>` argument when the command carried a subject, and otherwise from the first message typed into the wizard. Everything here is fire-and-forget and retries on the next send: a failed name leaves the tab as it was, never blocks a message, and must always clear `isGeneratingName` or the tab spins forever.
- **Audio/video playback:** media NEVER becomes a file preview tab. `handleOpenFileTab()` (`src/renderer/hooks/tabs/internal/useFilePreviewTabHandlers.ts`) is the single choke point every open path funnels through, and it diverts playable media to `useMediaPlaybackStore.openMedia()` (or `enqueueMedia()` when the caller passes `mediaMode: 'queue'`) before a tab can be created. Any path that opens SEVERAL files must pass `mediaMode: 'queue'` for every media file after the first - see `openFilesInOrder()` in `useFileContextMenu.ts` - or each open steals the player from the one before and only the last survives. The only surface media appears on is `FloatingMediaPlayer` - there is no docked/in-panel placement, so do NOT add one. Exactly ONE player exists app-wide, owned by `MediaPlaybackHost` (`src/renderer/components/MediaPlayback/`), mounted once in `App.tsx` and never unmounted: anything rendered per-tab or per-agent is torn down on switch, and removing a media element from the document pauses it. Do NOT mount a second player to play two files at once - the single-player invariant is what makes overlapping audio impossible; `stepMediaItem()` walks the queue in open order, a finished file hands off via `advanceAfterEnded()`, and the history menu jumps by recency. **Queue and history are two lists with opposite lifetimes:** the queue persists (settings key `mediaPlayerQueue`, debounced writes, hydrated in `settingsStore`) while history is per-boot, so history holds whole `MediaItem`s rather than IDs into the queue - never "simplify" it back into pointers, or dropping a queue entry would rewrite what the user already heard. Classify media with `getOpenedMediaKind()` in `src/renderer/utils/mediaItems.ts`. **A `file://` link clicked in markdown must go through `openFileUrl()`** (`src/renderer/utils/openFileUrl.ts`), never straight to `shell.openPath`: the file-link plugins emit `file://` for any path outside the project root, so an agent mentioning `~/Scratch/x.mp3` was handing it to the OS player and bypassing the choke point entirely. **The player's height is derived, never stored** (`src/renderer/utils/mediaFloatGeometry.ts`): chrome for audio, chrome + `width / aspect` for video, where the aspect is the file's real `videoWidth / videoHeight` and the chrome is MEASURED (font metrics differ per platform, so a hard-coded transport height letterboxes video somewhere). Only position and a per-kind width persist, so a mixed queue reshapes itself as it advances. **Minimize and close are different actions and must stay that way:** the `-` button sets `dismissed` (widget hidden, audio continues, `NowPlayingIndicator` in the Left Bar header becomes its play/pause via `requestToggle` plus a separate restore button), while `x` calls `closeItem` (player released, sound stops, rest of the queue intact). Minimize is a `hidden` STYLE FLAG on `FloatingMediaPlayer`, never a different render branch in `MediaPlaybackHost` - moving the media element in the React tree unmounts it, and that runs the spec's pause steps. **Neither list shows the loaded track:** history records DEPARTURES (a track enters when it stops being active) and `historyForActiveChange` also strips the incoming id, while the queue menu filters via `upcomingMediaItems()` at display time - the active item must stay in `items` or `stepMediaItem` loses prev/next. `clearQueue` keeps the loaded track, since clearing what is queued must not stop playback. A control that hides itself must not silently stop media, and a close button that only hid it leaves audio coming from nowhere. User docs: [media-player.md](docs/media-player.md).
- **Run a shell command from the chat:** `dispatchShellCommand()` (records it in recall history, then runs it) / `runShellCommand()` / `cancelShellCommand()` / `resolveCommandCwd()` in `src/renderer/services/shellCommand.ts`. This is command mode (`!command` in the AI composer). Any new surface that runs a one-off command and shows its output in a transcript should reuse this rather than calling `window.maestro.process.runCommand` directly - the synthetic `{sessionId}-shell-{runId}` id is what keeps the output out of the agent listeners. AI command mode routes its accepted suggestion through the SAME `dispatchShellCommand`, so it runs, records, and recalls identically; do NOT add a parallel run path for it. The one difference is the optional `request` field, stamped onto the card only when the caller passes one - its presence means exactly "this was generated, not typed", and it is what the card shows as provenance and what the next AI command sees as the `Asked:` line. `dispatchShellCommand` forwards its whole options object to `runShellCommand` rather than naming fields, so a new field cannot be silently dropped in between. See [RENDERER-SERVICES.md](docs/agent-guides/RENDERER-SERVICES.md#shellcommandts-200-lines).
- **Revealing output the user asked for, past a paused auto-scroll:** `requestTranscriptScrollToBottom(sessionId, tabId)` and `TRANSCRIPT_SCROLL_TO_BOTTOM_EVENT` in `src/renderer/services/transcriptScroll.ts`. The transcript follows new output only while auto-scroll is live; scrolling up to read history pauses it, and from then on new entries land offscreen behind the unread badge. That is correct for output the AGENT produced on its own schedule, and wrong for output the user just asked for by pressing Enter - a bang command's card starts streaming immediately, and a command whose output you cannot see has not really run. `TerminalOutput` owns the pause in LOCAL state and is mounted several levels below the composer, so the request rides one app-level `CustomEvent`, the same shape as `requestHeadingPalette` and `requestFileTreeRefresh`, rather than a callback drilled up through `MainPanel` and `App`. Four rules make it safe. **Fire it AFTER the content exists** - `runShellCommand` appends the card first, or the transcript scrolls to a bottom that does not include it yet. **Fire it ONCE per run, at the top**, never per output chunk: from there the transcript's own follow-the-tail handles the stream, and a scroll up mid-run pauses it as usual - a per-chunk request would pin the user to the bottom and make the transcript unreadable while a chatty command runs. **The receiver flips `autoScrollPausedRef` / `isAtBottomRef` BEFORE the `setState` calls**, because the MutationObserver reads the live refs in the same frame and would otherwise stall on the first mutation after the jump. **The detail names its session AND tab**, and a mismatch is ignored, so a command dispatched into a background tab cannot yank the view off what the user is reading. Do NOT reach for this to force ordinary agent output into view - the pause exists precisely to stop that.
- **Delete the file the user is previewing:** `requestFileDeletion({ path, sshRemoteId?, sessionId? })` in `src/renderer/services/fileDeletion.ts`. Opens the shared destructive `confirm` modal, deletes through `window.maestro.fs.delete` (SSH-aware, same IPC the Files panel uses), force-closes every preview tab pointing at the path, and nudges the Files panel through `requestFileTreeRefresh()`. Both the File Preview toolbar's trash button and the command palette's `File: Delete` entry route through it - do NOT hand-roll a second confirm-then-delete path, or the two surfaces drift on what they warn about and what they clean up. See [RENDERER-SERVICES.md](docs/agent-guides/RENDERER-SERVICES.md#filedeletionts---delete-the-previewed-file).
- **Telling the Files panel a file appeared or vanished:** `requestFileTreeRefresh(sessionId)` in `src/renderer/utils/fileTreeRefresh.ts` (renderer) and `nudgeFileTreeForPaths(paths)` in `src/cli/services/file-tree-refresh.ts` (CLI). The panel refreshes on a TIMER, so anything that writes or deletes outside that cadence leaves the tree describing a directory that no longer exists, and the user reads that as the save having done nothing. Both ends raise the same `maestro:refreshFileTree` event, whose single app-level listener lives in `useAppRemoteEventListeners` - which is the point: a deeply-nested hook nudges the tree without anyone prop-drilling `refreshFileTree` down to it. Do NOT hand-roll another `new CustomEvent('maestro:refreshFileTree', ...)`; there were four copies with four different comments. Fire it from the function that DID the write (`saveImageToProject` does), not from each caller, so a new surface is covered the moment it renders. The CLI half is deliberately silent and never throws: the bytes are already on disk, so a closed desktop must not turn a good write into a failed command - the loud form is `refreshFileTreeFor()`, behind `maestro-cli refresh-files`, where the refresh IS the command.
- **Loading a LOCAL file tree:** `walkLocalFileTree()` in `src/main/utils/file-tree-walk.ts`, reached from the renderer as `window.maestro.fs.readDirTree` and wrapped by `loadFileTree()` in `src/renderer/utils/fileExplorer.ts`. The whole walk happens in ONE round-trip. Do NOT recurse with `window.maestro.fs.readDir` from the renderer: that costs an IPC round-trip per directory, and on a large tree those round-trips - not the disk - are the entire load time, because each one waits its turn on a renderer main thread that may be busy rendering a streaming transcript. A vault worth about 100ms of real filesystem work sat on "Loading files..." for minutes, while the footer counts appeared immediately because `fs:directorySize` had always walked the tree in a single main-process call. SSH trees are the exception and keep the renderer walk, since a remote tree is batched separately through `listTreeRemote`. The traversal rules (ignore patterns, optional root `.gitignore`, depth cap, entry cap that folders are exempt from, and an always-visible `.maestro` subtree loaded in full past the cap) live in the walker, so a rule added on one side only is a rule local and SSH trees disagree about.
- **Ask the model for a shell command (AI command mode):** `requestAiCommand()` / `acceptAiCommand()` / `dismissAiCommand()` in `src/renderer/services/aiCommand.ts`, state in `src/renderer/stores/aiCommandStore.ts`, prompt in `src/prompts/ai-command.md` (id `ai-command`, editable in Settings -> Maestro Prompts). The suggestion runs on the tab's OWN provider at its current model and effort, read-only and with tools disabled. Do NOT hand-roll another one-shot model call for this: the main-process handler (`src/main/ipc/handlers/aiCommand.ts`) goes through the shared `groomContext()` runner.
- **The system-prompt envelope for a provider with no `--append-system-prompt`:** `EMBEDDED_SYSTEM_PROMPT_SEPARATOR`, `embedSystemPromptInPrompt(systemPrompt, userPrompt)`, `stripEmbeddedSystemPrompt(text)`, `hasEmbeddedSystemPrompt(text)` in `src/shared/embeddedSystemPrompt.ts`. Only `claude-code` declares `supportsAppendSystemPrompt`; for every other provider the spawn path has nowhere to put system content except the FIRST user turn, so it sends the whole Maestro system prompt, a `---` rule, and a `# User Request` heading ahead of what the human typed. The provider writes that turn to its session file verbatim, so the envelope is a WIRE FORMAT: changing the separator orphans every transcript already on disk, and a test pins the exact string. Both producers (`src/main/ipc/handlers/process.ts` for the desktop spawn, `src/cli/services/agent-spawner.ts` for the CLI) and the reader (`transcriptMessagesToLogEntries`) go through this module, because a third hand-built copy on the reading side is free to drift from the two on the writing side - which is exactly how a hydrated tab came to render the system prompt, the conductor profile, the cwd and the agent's own id as if the user had typed them (issue #1533). The strip is deliberately conservative: it honours only the FIRST separator (so a user request that quotes the marker survives), it requires something to precede it (a message that OPENS with `# User Request` was never wrapped, and stripping there would delete the user's first line), and only USER turns are unwrapped (an agent explaining the format keeps its answer). Do NOT template the envelope inline at a new spawn site, and do NOT strip it at a call site above `transcriptMessagesToLogEntries` - resume and backfill have to produce byte-identical entries for the same message or the backfill cannot find its splice point. Known gap: the Agent Sessions browser (`SessionMessagesView`) renders the raw messages and still shows the envelope.
- **Appending to a transcript entry:** `canAppendToLogEntry(entry, source)` / `isSelfContainedCard(entry)` in `src/renderer/utils/logEntries.ts`. A `LogEntry` is either an open stream (coalesce consecutive chunks into it) or a self-contained card that owns its own text (`shellCommand`, `retryOutageId`, `recoveryAction`, `aiCommand`, `metadata.hiddenProgress`, `metadata.toolState`). Cards keep a NATURAL source - a `!` command's card is `source: 'stdout'` because its body really is terminal output - so **never gate coalescing on `source` alone**; that's how agent replies ended up rendered inside a command's output box. One card has no marker at all: Claude's plan-limit banner arrives as ordinary `stdout` text on the `result` envelope, after the failure that started the retry was already raised, so `isSelfContainedCard` recognizes it by text via `isClaudeLimitNotice()` (`src/shared/agentErrorPatterns.ts`) - which is anchored and length-capped, so an agent merely _discussing_ limits still coalesces normally. Without that, the answer the retry produces half an hour later renders with "You've hit your session limit - resets ..." glued to its front, and transcripts already on disk are fixed too. Adding a new card kind means adding its marker to `isSelfContainedCard`, which fixes every coalescing site at once.
- **Command mode (`!`) is STATE, not a text prefix, and it is a LADDER:** the live value is `composerInputStore.aiCommandMode` (`'off' | 'shell' | 'ai'`), persisted per tab as `AITab.commandMode`. `!` on an EMPTY composer climbs one rung (agent -> `'shell'`, a literal command line -> `'ai'`, a plain-English request the model turns into one); Escape on an empty composer climbs back down; the caret never leaves the textarea. The bang is consumed on entry and never appears in the draft, so NEVER test the text for a leading `!` to detect the mode - a real command can contain bangs, and the draft won't start with one. **Never test the value for truthiness either: `'off'` is a truthy string.** Compare against the literals, or use `isShellCommandMode()` / `isAiCommandMode()`. Persisted values from older builds are booleans (`true` meant `'shell'`), so always read `AITab.commandMode` through `normalizeComposerCommandMode()`. The invariant: the same string is a shell command, a request for one, or an agent message depending only on this value, so any path that persists or restores `inputValue` MUST carry `commandMode` with it (`syncAiInputToSession` reads it from the store itself so a caller can't forget). Helpers all live in `src/renderer/utils/shellCommandInput.ts`. See [RENDERER-SERVICES.md](docs/agent-guides/RENDERER-SERVICES.md#command-mode-is-state-not-a-text-prefix).
- **Shell tab completion:** `useTabCompletion()` in `src/renderer/hooks/input/useTabCompletion.ts` serves BOTH the terminal composer and command mode, told apart by its third `commandMode` argument (the text can't say which - a command line looks the same either way). Terminal mode completes against `shellCwd` + `shellCommandHistory`; command mode completes against the project root + the bang-prefixed entries in `aiCommandHistory`, because a command-mode command runs at the agent's `cwd` regardless of where a terminal tab has `cd`-ed. Suggestion values are plain command lines in both cases. Add completion sources here, not in a parallel hook.
- **Saving generated image bytes to disk:** `saveImageDataUrlToDisk(dataUrl, defaultName?)` in `src/renderer/utils/imageExport.ts`. The sibling `saveImageElementToDisk()` exports an image that is already in the DOM; this one takes bytes produced on the fly (a page screenshot, a canvas render) that have no element to point at. Both go through the native save dialog and then `fs.writeImageFile` - `fs.writeFile` is UTF-8 and would corrupt raster bytes - and both fall back to a browser download when the dialog is unavailable (web renderer). A cancelled dialog reports `{ saved: false }` with no `error`, so a caller must not treat every non-save as a failure toast. Unlike `saveImageToProject()` it does not call `requestFileTreeRefresh`, because the user usually points it outside any workspace.
- **Screenshotting a surface exactly as it is painted:** `window.maestro.shell.capturePage(rect?)` (preload in `src/main/preload/system.ts`, handler `window:capturePage` in `src/main/ipc/handlers/system.ts`). Reach for it instead of serializing a canvas or rasterizing DOM whenever the thing on screen is composed of several render paths - the Document Graph is a `<canvas>`, a React markdown preview pane, and an HTML legend, and only the compositor knows what the user is actually looking at. Three things the handler already solves, so do not re-solve them: it captures `event.sender` rather than the main window, so a surface opened in a second window shoots itself; it floors and clamps the rect, because `getBoundingClientRect()` returns floats and Chromium answers an out-of-range region with an empty image; and it resolves `null` rather than an empty data URL when there is nothing to capture. Callers must dismiss any modal of their own and wait two animation frames first, or the shot includes the dialog that asked for it. Feature-detect (`!!window.maestro?.shell?.capturePage`) before offering the affordance - the web renderer has no bridge.
- **Right-click menu on an image:** already done, app-wide. `<ImageContextMenuHost>` (`src/renderer/components/ImageContextMenuHost.tsx`) is mounted once in `App.tsx` and runs ONE delegated `contextmenu` listener that resolves the image from the click target, so raster `<img>`, inline `<svg>`, Mermaid charts, thumbnails, and the lightbox all get Copy Image / Save to Project / Save As with **zero per-surface wiring**. Do NOT add an `onContextMenu` to a new image surface or hand-roll copy/save buttons - a new surface is covered the moment it renders. To opt a surface out (it owns its own right-click behavior), put `data-no-image-menu` on its container. Export helpers live in `src/renderer/utils/imageExport.ts` (`saveImageToProject()` writes to `.maestro/diagrams/` and works over SSH). See [UI-PATTERNS.md → Right-Click Image Menu](docs/agent-guides/UI-PATTERNS.md#right-click-image-menu-imagecontextmenuhost).
- **Font zoom on a reading pane:** `useFontScale(storageKey)` in `src/renderer/hooks/ui/useFontScale.ts` + `<FontScaleControl>` in `src/renderer/components/ui/FontScaleControl.tsx` (`variant="inline"` for a toolbar, `variant="floating"` for a pill over a scrolling pane). The hook owns clamping, two-decimal rounding, and localStorage; do NOT hand-roll another `AArrowUp`/`AArrowDown` pair or another `clampFontScale`. Render it ONLY where the zoom moves type - Director's Notes hides it in Rich Mode, the file preview gates it on `canScaleFontForView()` in `filePreviewUtils.ts`, and Auto Run only floats it once the folder actually has a document. A floating pill is pinned with a `sticky top-* z-20 h-0` row, not `absolute`, so it needs no positioned ancestor and never displaces the content it sits over. See [UI-PATTERNS.md → `<FontScaleControl>`](docs/agent-guides/UI-PATTERNS.md#fontscalecontrol-srcrenderercomponentsuifontscalecontroltsx).
- **Bare `+` / `-` / `0` zoom on a surface:** `useScaleShortcuts(control, { enabled })` in `src/renderer/hooks/ui/useScaleShortcuts.ts`, over any `useScalePreference` control, with the keys named in the buttons' tooltips via `<ScaleControl shortcutHint>`. It is deliberately MODIFIER-FREE and skips any event carrying Cmd/Ctrl/Alt, because `Cmd+=` / `Cmd+-` is the app-wide font zoom and must keep working while a zoomable surface is open. It listens on `window` at CAPTURE rather than on the surface's node: focus falls to the body whenever a nested overlay closes, so a node-scoped handler goes dead exactly when the user comes back to the surface, and `stopPropagation` is what keeps a bare `0` or `-` out of the global shortcut handler. **Gate it with `enabled`, normally `useIsTopLayer(priority)`** - two stacked surfaces both answering the same unmodified key is the failure this replaces. `FilePreview` keeps its own inline copy of the same branch because that one sits inside a single ordered key chain where position decides which branch wins; every other surface rides the hook.
- **Whether a surface is the topmost layer:** `useIsTopLayer(priority)` in `src/renderer/hooks/ui/useIsTopLayer.ts`. Matching is by `MODAL_PRIORITIES` value rather than layer id, because `useModalLayer` owns the registration and never hands the id back. Reach for it whenever a surface binds an unmodified key or wants to reclaim focus (`AutoRunExpandedModal`) - do NOT re-derive `getLayers()[length - 1]` inline.
- **Who asked for this turn (interactive vs automation):** `QUERY_SOURCE_ENV_VAR`, `QuerySource`, `DEFAULT_QUERY_SOURCE` in `src/shared/querySource.ts`. Every agent turn Maestro runs - one the user typed, an Auto Run task, a Cue subscription firing - is the same thing on the machine: an ordinary agent process, same binary, same args, same cwd, writing to the same provider transcript. Nothing outside Maestro can tell them apart, so tooling that hangs off the agent (Claude Code hooks, wrapper scripts, telemetry sidecars) is left guessing from prompt text - which is not an answer, because Cue prompts are the user's own words out of `cue.yaml` and read exactly like something a human typed. So Maestro states it: every spawn stamps `MAESTRO_QUERY_SOURCE` into the child env. Set it by CLAIMING the turn at the dispatch site (`querySource: 'auto'` on the Auto Run spawn, `'cue'` in `cue-spawn-builder`), never by inferring it downstream. It is stamped AFTER the user-editable env layers in `buildChildProcessEnv()` on purpose: a stray global var of the same name would otherwise silently relabel every turn on the machine, and the marker is only worth having if it is trusted. **SSH needs it re-stated**: only `customEnvVars` crosses to the remote host, so the local process env never gets there - `wrapSpawnWithSsh` takes `querySource` and exports it, and any new SSH spawn site must pass it or every remote turn arrives looking hand-typed. Terminal PTYs deliberately do NOT carry the marker (a shell is not an agent turn), which is why `collectMaestroEnvVars()` only reports it when the caller resolved one - that list mirrors what the process actually received and must not advertise a variable the user would then fail to find. The value describes ORIGIN, not autonomy: a `user` turn whose prompt says "go fix everything" is still `user`, and ties break toward the human because over-reporting delegation would flatter the numbers.
- **An agent's effective environment:** `resolveAgentEnvironment()`, `isSecretEnvKey()`, `maskEnvValue()`, `envSourceLabel()` in `src/shared/agentEnvironment.ts`, rendered by `<EnvVarList>` in `src/renderer/components/ui/EnvVarList.tsx`. Two layers decide what an agent runs as, edited in different panes, so no single settings screen answers "which profile is this actually running as?": global (Settings -> Environment) underneath, and on top exactly ONE user-configured set - the agent's own vars, or the per-provider set (Settings -> Agents) when the agent has no vars record at all. The agent's record REPLACES the provider set rather than merging over it, even when it is empty, because that is what `applyAgentConfigOverrides()` hands the process; a merged view shows a provider `CLAUDE_CONFIG_DIR` beside an agent's `ANTHROPIC_API_KEY` that the agent never receives. The module does the SAME resolution as the spawner and reports which layer won plus which it shadowed. Do NOT re-derive the precedence inline: a merge that disagrees with the spawner's describes a process nobody is running. Distinct from `Settings/EnvVarsEditor`, which EDITS one layer - pick by question ("change a value" vs "which profile am I on?") and do not add an edit mode to `<EnvVarList>` to cover both. **A variable switched off with the editor's eye button lives in a SECOND record** (`shellEnvVarsDisabled`, same shape as `shellEnvVars`), and nothing but the editor may read it: parking is what keeps the value recoverable without it reaching a process, so being present in `shellEnvVars` is exactly the same statement as being live, and every consumer stays filter-free. Do NOT merge the disabled record into a spawn env or teach a spawner to skip entries - a single `enabled` flag inside one record would put that filter in every one of the dozen-odd read sites, and the first one that forgets it silently ships the API key the user thought they had switched off. Secret-looking keys are masked behind a per-row reveal, matched loosely on purpose: these panels get opened while screen-sharing for help, so a false positive costs one click and a false negative leaks a live key.
- **Whether a configured env value means "unset":** `isBlankEnvValue(value)` and `stripBlankEnvVars(vars)` in `src/shared/agentEnvironment.ts`. A blank field in an env editor is an ABSENCE, not a value, and the two are not interchangeable to the child: exporting `FOO=` hands over a set-but-empty variable, so code that reads `process.env.FOO` as a path gets `''` instead of falling back to its own default. That is how a blank `CLAUDE_CONFIG_DIR` made Claude Code call `mkdir('')` and die before it reached the provider. Every spawn path therefore drops blanks: `collectMaestroEnvVars()` and `buildChildProcessEnv()` in `src/main/process-manager/utils/envBuilder.ts`, and `withQuerySource()` in `src/main/utils/ssh-spawn-wrapper.ts`. Two rules when you touch a spawn env. **Merge the layers first and strip second** - dropping a blank before the merge leaves the lower layer's value in place, so a blank session override could not cancel a global one, which is the main reason a user blanks a field at all. And **`delete env[key]`, do not merely skip the assignment** - the child inherits `process.env`, so skipping leaves the inherited value live and the "unset" wins nothing. Deliberately DIFFERENT from `resolveAgentEnvironment()` above, which KEEPS blanks because it reports what the user CONFIGURED and the env viewer has to show the blank row that is doing the cancelling. Pick by question: "what did they type?" versus "what does the process actually get?".
- **Suggesting an env-var NAME:** `suggestEnvVarKeys()`, `PROVIDER_ENV_VAR_SUGGESTIONS`, `COMMON_ENV_VAR_SUGGESTIONS`, `rememberableEnvVarKeys()` in `src/shared/envVarCatalog.ts`, rendered by `<EnvVarKeyInput>` in `src/renderer/components/shared/EnvVarKeyInput.tsx` and fed by `useKnownEnvVarKeys()` (`agents:getKnownEnvVarKeys`). A mistyped name fails SILENTLY - `CLAUDE_HOME` is set, the CLI ignores it, and the agent runs as if nothing was configured - so the name field offers the provider's own vars plus every name the user has already set anywhere in Maestro. Two rules if you extend it. **Add a provider by adding one entry to `PROVIDER_ENV_VAR_SUGGESTIONS`**, not by special-casing an editor: both the per-agent panel (`AgentConfigPanel`) and Settings -> Environment (`EnvVarsEditor`) read the same function, and the global editor deliberately offers EVERY provider's catalog because it has no provider of its own. And **remembered names are derived, never stored** - the handler reads the keys already on disk in the agent configs, the sessions, and `shellEnvVars`, skipping blank-valued rows so a half-finished `VAR` row from the Add Variable button is never suggested back. Nothing new is persisted, and no VALUE ever crosses the IPC boundary, which is what keeps a remembered `ANTHROPIC_API_KEY` from becoming a second copy of a credential. A new row is added with NO name at all (`withBlankEnvVarRow()` / `BLANK_ENV_VAR_KEY`, the one place that rule lives, replacing six copies of a `NEW_VAR_n` placeholder loop): a placeholder name is something the user has to select and delete, and it also filters the suggestion list down to nothing, so the field would open on an empty list. The editor autofocuses that row and opens the list, keyed on an explicit "user pressed Add" flag rather than on "this row is blank", so a blank row restored from disk never steals the caret on modal open. The unnamed row lives in the same record as the real ones, so every spawn path drops it via `isBlankEnvKey()` in `agentEnvironment.ts` - `env[''] = 'x'` is a variable no child can read, and Windows rejects the empty name outright.
- **Whether a login flow can fix an auth failure:** `classifyCredentialKind()` and `credentialKindBlocksLogin()` in `src/shared/providerAuthIdentity.ts`, which also owns `providerAuthKey()` (the credential's shared scope: agent binary + host). NOT every credential is an OAuth login. An agent authenticating with `ANTHROPIC_API_KEY`, one pointed at a gateway via `ANTHROPIC_BASE_URL`, and one running on Bedrock/Vertex all fail with the SAME `auth_expired` output an expired login produces, and running the provider's login command for them produces a successful-looking flow that changes nothing the agent presents - the user believes it is fixed and the next prompt burns on the same rejection. So any surface that offers to sign the user in gates on this first (`ReauthModal` does), and reports the real remedy instead. Feed it the effective env from `resolveAgentEnvironment()`, never a partial one: a classification built from a different merge describes a process nobody is running. Related: an error `message` in `src/shared/agentErrorPatterns.ts` must name WHAT failed and never a terminal command, because the bank does not know which of these kinds produced the line.
- **Typing a login command into a shell:** `formatAgentLoginCommand(login, syntax?)` and `loginShellSyntaxFor(shellId, isWindows)` in `src/shared/agentMetadata.ts`. Rendering the line is not string concatenation, because the shell it lands in changes what runs. PowerShell parses a line that STARTS with a quoted string as an expression and echoes it, so `"C:\Program Files\Claude\claude.exe" /login` prints a path and logs nobody in; the call operator `&` is what makes it a command, and only PowerShell wants it - cmd.exe runs the quoted path directly. Windows agents installing under `C:\Program Files\...` makes this the common case there, not an edge case. So ask `loginShellSyntaxFor` for the dialect rather than sniffing the shell id at the call site: off Windows everything is `posix`, and ON Windows Git Bash and WSL are still `posix` while an unset or unknown shell falls back to `powershell`. Two related rules live with the caller (`ReauthModal`): the line is submitted with **CR, not LF** (ConPTY delivers LF as Ctrl+J, which PSReadLine ignores), and a Windows `wsl` default shell is replaced with `powershell`, because agents spawn as native processes and a login inside WSL writes credentials the agent never reads. An SSH remote is exempt from both - its shell is the remote host's.
- **Bucketing Director's Notes bullets:** `bucketNarrativeItems()` / `shouldRenderBuckets()` / `buildNarrativeGroupLookup()` in `src/shared/directorNotesGrouping.ts`, bound to live store state by `useNarrativeGroupLookup()` in `src/renderer/components/DirectorNotes/`. The synopsis agent emits a flat list of bullets per section, each tagged with the session it came from; the GROUP that session belongs to is Maestro's answer, never the model's, which is why it is derived from `sessionStore` instead of being one more field the model can invent. Two surfaces render the same narrative - Rich Mode (`NarrativeSections`) and the markdown path behind Plain Mode / Copy / Save (`narrativeToMarkdown`) - so the bucketing rule lives in one pure module they share; a second copy drifts on the parts that are easy to get subtly wrong: an agent name is matched through `normalizeAgentKey` (the manifest hands the model a sanitized display name, so it rarely matches a stored name byte for byte), a blank `agent` string means the same thing as no agent, the unattributed catch-all always sinks last, the agent pill survives only inside a GROUP bucket (under an agent header it just repeats the header), and a section with ONE bucket stays a flat list because a lone subheading only restates the section title. Do NOT add a `group` field to the prompt contract in `src/prompts/director-notes.md` - the model's job ends at naming the session.
- **Which provider generates a Director's Notes synopsis:** `AUTO_SYNOPSIS_PROVIDER` / `synopsisProviderChoice()` / `pickFirstAvailableProvider()` / `SYNOPSIS_PROVIDER_PREFERENCE` in `src/shared/directorNotesProvider.ts`, resolved to a concrete agent by `resolveSynopsisProvider()` in `src/main/utils/director-notes-provider.ts`. THREE surfaces start a synopsis - the desktop IPC handler, the web-server callback that the CLI drives, and (through it) `maestro-cli director-notes synopsis` - and before this they each hand-rolled `getAgent(provider)` plus their own "not available" copy, so a fourth caller would have written a fourth. `'auto'` is a SENTINEL ON THE WIRE, not a stored provider id: the renderer, CLI, and web client send the string and the MAIN PROCESS decides, because only it can see which agents are actually installed at the moment the run starts. That is the whole point - a conductor whose Claude OAuth expired still gets a synopsis from Codex without opening Settings. `DirectorNotesSettings` keeps the mode (`autoSelectProvider`) and the manual pick (`provider`) in SEPARATE fields, so turning auto off restores what the user last chose instead of snapping back to the first entry in the list; `undefined` counts as auto so installs predating the setting get the new behavior. An EXPLICIT provider that is unavailable is an error, never a silent fallback - the user named that agent, and quietly running somewhere else hides a broken install. Per-provider overrides (`customPath` / `customArgs` / `customEnvVars`) belong to the manual pick and are WITHHELD under auto: applying Claude Code's binary path to whichever agent auto lands on is worse than sending nothing. The preference order mirrors the wizard's `AGENT_TILES` but is redeclared in `shared/` because the main process cannot import renderer modules; `terminal` and the unsupported agents are absent on purpose, since a synopsis is a one-shot batch prompt. Do NOT add a second "first available" scan - the Feedback modal has its own older copy in `FeedbackChatView.tsx` and that one is not the canonical answer.
- **Sortable table header:** `<SortableTh>` in `src/renderer/components/ui/SortableTh.tsx` + `useTableSort()` in `src/renderer/hooks/ui/useTableSort.ts`. The hook owns the flip-vs-switch rule (clicking the ACTIVE column reverses it; clicking a DIFFERENT column jumps to that column's own default direction instead of inheriting the previous one - inheriting is what makes a freshly-picked column show Z-A or least-used-first and read as broken data). The component owns `aria-sort` placement (on the `<th>`, not the control), a stable caret slot so switching columns doesn't reflow the header row, and a real `<button>` for the click target. Do NOT hand-roll `<th role="button" onClick>`: `role` grants the semantics without the tab stop or Enter/Space handling, so it announces as a button and then does nothing from the keyboard.
- **Graphing a set of documents rather than one:** `BuildOptions.scopeFiles` / `scopeDirectory` in `src/renderer/components/DocumentGraph/graphDataBuilder.ts`, opened via `openGraphScope()` in `fileExplorerStore.ts`. Focus mode ("what does this document reach?") only creates nodes for files its BFS walks INTO, so a file nothing links to can never appear; scope mode ("how do these documents relate?") makes a node for every file in the set. Unreachable documents were being dropped TWICE - the builder's BFS, and then again by `prepareLayoutInput()` in `mindMapLayouts.ts`, which all six layout algorithms funnel through - so fixing only the builder renders nothing. They ride a `showOrphans` flag into the layout and get their own band via `positionOrphanNodesBottom()`, NOT a borrowed depth ring: an unreachable node has no depth, and giving it one draws it as a child of a center it has no link to. Scope mode also skips `startBacklinkScan`, which would pull in files outside the scope - the exact files the user did not select.
- **A labeled action button in a panel header:** `<HeaderActionButton>` in `src/renderer/components/ui/HeaderActionButton.tsx` - the "+ New Memory" / "+ New Session" / "Resume" pill. It was hand-rolled in five files as the same `px-3 py-1.5 rounded-lg text-sm font-medium` string. It sizes ONE STEP BELOW the header title (`text-xs`, icon `w-3.5`), deliberately: the copies used `text-sm`, which is exactly the title's size, so the button read as heavy as the heading it sits beside and dominated a header it is subordinate to. Pass the icon without size classes - the component sizes it. Do NOT hand-roll another accent pill, and do not "fix" it back to `text-sm`.
- **Highlighting a search hit:** `highlightMatches(text, query, accentColor)` in `src/renderer/utils/highlightMatches.tsx` for ordinary text, `searchMatchRanges(text, query)` in the same file for the `{from, to}` byte ranges a CodeMirror `setSearchMatches()` wants, and `<TextareaHighlightOverlay>` in `src/renderer/components/ui/TextareaHighlightOverlay.tsx` for text inside an editable `<textarea>`. All three split on the shared `splitOnMatches()`, so the regex escaping and the match parity cannot drift between them - which is the whole point in a pane offering BOTH a rendered preview and a source editor, where two matchers would let the two halves disagree about what is a hit. Do NOT re-derive offsets by walking the text again; `splitOnMatches` already carries each segment's `start`. A textarea renders plain text and nothing else, so the overlay is a backdrop UNDER it whose own glyphs are transparent - the visible characters are always the real editable ones and only the mark backgrounds show through, which keeps the caret, selection, undo stack, and IME intact. Its font, padding, and border width are COPIED FROM THE LIVE COMPUTED STYLE rather than restated in CSS: a duplicated font rule works right up until someone edits the textarea's and not the overlay's, and every highlight then lands a few pixels off the word it belongs to. Do NOT swap the textarea for a contenteditable to get markup - that trades a one-off alignment problem for a permanent input-handling one.
- **Highlighting a FUZZY hit:** `renderFuzzyHighlight(text, indices, styles?)` in `src/renderer/utils/search.ts`, fed by that file's `fuzzyMatchWithIndices()`. This is a DIFFERENT problem from `highlightMatches` above, and reaching for the wrong one silently paints the wrong characters. A substring search hit is one contiguous run, so `splitOnMatches` can mark it; a fuzzy hit is a scattered subsequence (`oec` matching **O**PSWAT **E**quity **C**ase), so there is no run to wrap and the only honest highlight is per character. Pass the index set the MATCHER returned rather than re-deriving positions at render time - `fuzzyMatchWithIndices` prefers boundary-anchored positions and backtracks so the rest of the query can still match, and a naive first-occurrence scan disagrees with it on exactly the queries where the emphasis matters most. Riders: the slash-command popup (`highlightSlashCommand`, which shifts indices by one for the leading `/`) and the File Preview heading palette. Returns the plain string when the set is empty, so a no-query list costs no extra DOM.
- **Jumping a markdown preview to a heading:** `scrollToHeadingSlug(slug, container, behavior, onSelectHeading?)` plus `headingLevelColor(theme, level)` in `src/renderer/components/FilePreview/shared/headings.ts`. TWO surfaces navigate the same heading list - the Table of Contents overlay (`FilePreviewToc`) and the `#` heading palette (`HeadingPalette`) - and they must not disagree about what a jump does, because the preview has THREE render tiers and only one of them can be scrolled by slug. Rich and Giant mount every heading, so a `querySelector` on the slug is enough; the Fast tier virtualizes its blocks, so most headings are not in the DOM at all and the lookup silently no-ops. The `onSelectHeading` override is how the Fast tier scrolls by block index instead, and it returns a BOOLEAN rather than being tier-gated at the call site: `markdownFastRef` is null under the other two tiers, so "not handled" falls through to the DOM path on its own and no caller has to know which tier is mounted. `CSS.escape` is not optional - slugs come from heading text, so they routinely start with a digit (`#1-intro`), and a bare selector throws. `FilePreview` owns the single `jumpToHeading` callback and hands it to both surfaces; do NOT give a new heading surface its own `markdownContainerRef` and scroll logic. Keyboard navigation passes `'auto'` and clicks pass `'smooth'` on purpose: a held arrow key repeats faster than a smooth scroll animates, so every repeat cancels the one in flight and the list stalls.
- **Which heading the reader is under right now:** `findActiveHeadingSlug(scroller, container, onReadActive?)` plus `ACTIVE_HEADING_FOLD_PX` in `src/renderer/components/FilePreview/shared/headings.ts`. This is the read half of the jump above, and it has the SAME tier problem: Rich and Giant mount every heading so a DOM walk answers it, while the Fast tier virtualizes its blocks and its headings mostly do not exist in the DOM. The `onReadActive` override is how the Fast tier answers from its parsed block array (`getActiveHeadingSlug` on `MarkdownPreviewFastHandle`), and it is tri-state on purpose: a slug means "this one", `null` means "above the first heading" (a real answer, not a miss), and `undefined` means "no opinion" and falls through to the DOM walk, so callers never have to know which tier is mounted. The DOM walk BINARY SEARCHES: headings sit in document order so their tops increase monotonically, and this runs on every scroll frame - do not turn it back into a linear `find`. `FilePreview` owns the measurement because only it holds both the scroller and the container ref, throttles it into the existing scroll listener, and skips the work entirely while the overlay is closed. Do NOT let `FilePreviewToc` measure for itself.
- **Highlight vs selection in a heading list:** `FilePreviewToc` takes `activeIndex` (where the document is scrolled) and keeps its own `selectedIndex` (which row is lit). They are separate for two reasons: a click or arrow press must light the row IMMEDIATELY rather than waiting for the jump's scroll to land, and near the bottom of a document the scroll clamps so `activeIndex` stops changing - a list driven only by the document would freeze and the last few headings would be unreachable by keyboard. `-1` is a real value on both: it means the reader is above the first heading, so no row is lit and the `Top` sash gets the accent rail instead.
- **Reaching the open file preview from a modal:** `requestHeadingPalette()` and `HEADING_PALETTE_EVENT` in `src/renderer/services/headingPalette.ts`. The command palette and the File Preview are siblings that cannot see each other: the preview owns its overlays in local state, and its `filePreviewRef` is created inside `MainPanel`, three levels below the modal that wants to act on it. Worse, the palette is a modal whose whole job is TAKING focus from the surface it is about to operate on. So the request rides one app-level `CustomEvent` (the same shape as `requestFileTreeRefresh`) instead of drilling a callback up through `MainPanel` and `App`; exactly one `FilePreview` is mounted at a time, so there is no ambiguity about who answers. Two rules make it safe. The SENDER decides whether the command exists at all - `buildFilePreviewCommands` only offers "Jump to Heading" for a markdown tab in preview mode that actually has headings, so a palette entry never appears under different conditions than the `#` key it mirrors. The RECEIVER re-checks the same guards anyway, because the user can close or edit the file between opening the palette and picking the command. The action is also deferred by a tick: both are modals that focus their own search box on mount, and the closing one restores focus on its way out, landing on top of the box that just opened. Do NOT reach for this to make an ordinary parent-to-child call - it exists for the modal-to-sibling case where a ref genuinely cannot cross.

- **Opening the expanded staged-image organizer:** `requestOpenStagedImagesOrganizer()` and `OPEN_STAGED_IMAGES_ORGANIZER_EVENT` in `src/renderer/services/stagedImagesOrganizer.ts`. The organizer's open state is LOCAL to `StagedImagesStrip`, which sits several levels below the app-level keyboard handler that owns the `openImageOrganizer` shortcut, so the request rides one app-level `CustomEvent` in the same shape as `requestHeadingPalette` and `requestTranscriptScrollToBottom` rather than a callback drilled up through `InputArea` and `MainPanel`. It is fire-and-forget on purpose: the strip only mounts while images are staged, so a keypress with an empty composer is a silent no-op instead of an empty modal. Both ends check `stagedImages.length > 1` - the handler so the shortcut is not recorded as a tracked event when there is nothing to organize, the strip because it is the one that knows the live count. One image is the lightbox's job (`Cmd+Y`), not the organizer's. Do NOT hand-roll another dispatch of this event name, and do NOT prop-drill the setter instead: exactly one strip is mounted at a time, which is what makes the event unambiguous.
- **A text box that narrows a list:** `<FilterInput>` in `src/renderer/components/ui/FilterInput.tsx` - search icon, borderless input, optional `resultLabel` count, and a clear button that appears only once there is something to clear. Do NOT hand-roll another `<Search/> + <input>` pill; there are already a dozen near-identical copies that disagree on whether a clear button exists and what Escape does. Distinct from a FIND bar (`AutoRunSearchBar`, `TerminalSearchBar`), which walks matches inside one document and owns next/prev plus a match index - pick by question ("which rows do I see?" vs "take me to the next hit") and do not add match navigation to `<FilterInput>`. **Escape needs the host's help inside a modal:** the layer stack handles the key at capture on `window`, so the input never sees it and the surface closes instead of the filter clearing. Clear the query from the host's own `onEscape` first (see `MemoryViewer`). **Do NOT add a collapsed/magnifier-only mode to it** - that was tried and removed: hiding a pane's primary control to buy space in a crowded toolbar trades a layout problem for a discoverability one. When a toolbar cannot fit on one line, move what is NOT a control off that line (the Memory Viewer's corpus stats went to a footer).
- **A pane that reads and edits a markdown document:** the File Preview stack, never a bare `<textarea>`. `<Markdown preset="document">` for reading (scoped with `generateProseStyles({ theme, scopeSelector })` so its heading and table rules do not leak onto the surrounding chrome), `<MarkdownEditor>` from `src/renderer/components/FilePreview/markdownEditor` for writing (CodeMirror colouring, wrap-aware line-number gutter, imperative `focus` / `scrollToLine` / `setSearchMatches` / `getCaret` / `getSelectionRange` / `setSelection` / `replaceRange` / `getScrollTop` / `setScrollTop` handle), and `Cmd/Ctrl+E` to flip between them - read from the user's LIVE `toggleMarkdownMode` binding via `eventMatchesShortcutKeys`, never from a literal `e`, or the chord that flips a file preview and the chord that flips a memory drift apart after a remap. **Open in Preview**: these panes are opened to read far more often than to write. A pane registered through `useModalLayer` must bind the chord ITSELF (it blocks the app-level handler), must hand the caret to the editor on the way in and back to the list on the way out, and must key the editor on the filename - undo history belongs to one document, and carried across a switch an undo pastes the previous file's text into this one. Search highlights are PUSHED through `setSearchMatches()` from an effect rather than passed as a prop (CM6 owns its document; rebuilding the view discards the undo stack and the caret), built with `searchMatchRanges()` so the two modes cannot disagree about what is a hit. `readOnly` needs BOTH halves of the switch (`EditorState.readOnly` refuses edits, `EditorView.editable.of(false)` also drops the caret and the `contenteditable` attribute) - set only the first and you ship a pane that still looks like a text box and silently swallows typing. `onKeyDown` runs at `Prec.highest`, BEFORE CodeMirror's own keymap, and returning `true` swallows the key: that is how a host-owned popup claims the arrow keys, and returning nothing is the safe default. `onPaste` is the paste-side twin of `onKeyDown` - also `Prec.highest`, also `true` to swallow - which is how a host rewrites a paste (trimming whitespace, saving a clipboard image as an attachment) before CM6 inserts the clipboard text; it must answer SYNCHRONOUSLY, so claim the event first and finish the async work afterwards. `MemoryViewer` is the reference implementation; `MaestroPromptsTab` is the second rider and differs only where a prompt differs from a memory (it opens on `edit`, and Preview resolves `{{TEMPLATE}}` variables against the active agent first); the Auto Run panel is the third, and is the one that drives the handle hardest - tab insert, list continuation, checkbox insert, undo/redo, paste and scroll sync all go through `replaceRange` / `getSelectionRange` / `setScrollTop` rather than touching a DOM node. A host that edits through `replaceRange` comes back out through its own `onChange`, so an explicit edit that already pushed an undo entry has to mark the new content (Auto Run stamps `lastUndoSnapshotRef`) or that `onChange` schedules a second one. **Under test, CodeMirror cannot lay itself out** - jsdom has no `getClientRects` on a text range and CM6 throws - so any suite rendering a surface that contains the editor mocks the module with `markdownEditorModuleMock()` from `src/__tests__/helpers/mockMarkdownEditor.tsx`, a `<textarea>` double that implements the same handle against that textarea. Spell the `vi.mock` path relative to the TEST file. See [UI-PATTERNS.md → A Surface That Reads and Edits Markdown](docs/agent-guides/UI-PATTERNS.md#a-surface-that-reads-and-edits-markdown).
- **`{{template}}` variable autocomplete:** the shared state machine `useTemplateAutocompleteEngine()` in `src/renderer/hooks/input/useTemplateAutocompleteEngine.ts`, plus ONE binding per text surface - `useTemplateAutocomplete()` for a plain `<textarea>` (the command panels, the prompt composers) and `useEditorTemplateAutocomplete()` for the CodeMirror `MarkdownEditor` (Maestro Prompts, Auto Run). The two surfaces share nothing at the DOM level (a textarea has `selectionStart` and no per-character boxes, so its caret is located with a mirror div; CM6 has neither and reports coordinates from the view), but everything ABOVE the DOM is identical - when to open, what the query is, which key does what, and what text replaces what - so that half lives in the engine and each surface supplies a three-method `TemplateAutocompleteTarget` (`getCaret`, `getCaretPosition`, `replaceRange`), all of them about caret positions. Do NOT hand-roll a second `{{`-detector against a new editor: the copies drift on the parts that are subtle, namely that backing out past the trigger or typing `}}` by hand must CLOSE the popup, and that `selectedIndex` has to be re-clamped as the filter shrinks the list. The target is read through a ref rather than listed as a dependency, because its host rebuilds it every render and naming it would churn every callback's identity on each keystroke. On CM6 the binding must also return `true` from `onKeyDown` to claim Up/Down/Enter/Escape, or the editor's own keymap moves the caret first.
- **Keyboard navigation in a `<DualPaneFileEditor>` list:** built in. Up/Down walk the VISIBLE rows (collapsed categories are skipped, the ends do not wrap), and Backspace/Delete raise `onDeleteItem(selectedId)` - the list reports the intent, the consumer owns the confirmation. Both keys are ignored unless the event came from a row, so the "+ New" button in the same container cannot delete anything. Pass `autoFocusList` when the surface's whole job is walking the list (the Memory Viewer) so the keys work without clicking a row first; it fires on the FIRST selection rather than on mount (the list loads async) and defers to anything that already holds focus, so a user who went straight for the filter box keeps the caret. After a consumer-driven delete, bump `listFocusToken`: the focused row was just unmounted, focus falls to `<body>`, and the next Backspace silently does nothing, which reads as the keyboard dying halfway through a cleanup pass.
- **Segmented toolbar (sort/filter pill bar):** `<SegmentedControl>` in `src/renderer/components/ui/SegmentedControl.tsx`. The "Sort by: [Name][Created][Queries]" bar above a grid or chart. It owns active-segment coloring, seam borders, `role="radiogroup"` semantics, arrow-key navigation, and a single tab stop; each segment gets `data-testid="${testId}-${value}"`. Do NOT hand-roll another `.map()` over buttons with `borderLeft` seams. Distinct from `<RadioGroup>`, which is the stacked list-row form for settings panes - pick by layout, and do not add a `variant` prop to either to cover the other. See [UI-PATTERNS.md → Segmented Toolbars](docs/agent-guides/UI-PATTERNS.md#segmented-toolbars-segmentedcontrol).
- **Paginating a list already in memory:** `usePagination(items, pageSize, resetKey)` in `src/renderer/hooks/ui/usePagination.ts`, pure helpers in `src/renderer/utils/pagination.ts`, UI in `<Pager>` (`src/renderer/components/ui/Pager.tsx`). Do NOT confuse it with `useHistoryPagination`, which is the async IPC-backed windowing engine for data that arrives page by page - this one is for a list you already hold. The hook clamps the current page on every read rather than in an effect, which is what stops a filter that shrinks 1236 rows to 18 from rendering an empty grid under "Page 30 of 1"; pass a `resetKey` built from whatever the user can change that reorders the list (sort mode, filter mode, query text) so re-sorting does not strand them on an arbitrary slice. Render `<Pager>` in the TOOLBAR ROW, not under the list: a pager below a long grid inside a scrolling modal makes the user scroll to the bottom, click, then scroll back up.
- **Following streaming output in a capped box:** `useStickToBottom(contentKey)` in `src/renderer/hooks/ui/useStickToBottom.ts` - returns a callback ref that keeps a scrolling box pinned to its newest content and releases the moment the user scrolls up. Reach for it whenever a box has BOTH a capped height and content that arrives over time: once it hits its cap it stops growing, so the transcript's own auto-scroll has nothing left to follow and the user is stranded on the FIRST screen of output while the live tail piles up below. Pinning is recomputed from geometry on every scroll event, never remembered as a flag - a flag has to tell a programmatic scroll from a user one, and scrolling to where you already are fires no event to clear the guard. Do NOT collapse it into a boolean. Distinct from `useScrollIntoView` (one element in a list) and `TerminalOutput`'s MutationObserver (the whole pane). See [UI-PATTERNS.md → Following Streaming Output](docs/agent-guides/UI-PATTERNS.md#following-streaming-output-usesticktobottom).
- **Reaching an off-screen item while dragging:** `useDragAutoScroll(ref, { active, axis?, edgeSize?, maxSpeed?, startInset?, endInset? })` in `src/renderer/hooks/ui/useDragAutoScroll.ts`. Any scrollable strip that also accepts an HTML5 reorder drag needs this, or the drag can only ever reach the items already on screen: the pointer pins the container's edge, the drop target stops moving, and the user has to drop, scroll, and drag again. While `active` is true it records `dragover` positions on the container and scrolls on a rAF loop whenever the pointer sits in an edge band, ramping speed with how deep into the band it is so a nudge creeps and a pinned edge moves fast. Three details are what make it not worth rewriting inline: the insets are FUNCTIONS evaluated per frame, because a bar with sticky overlays (the tab bar's search cluster and its `+`) has its real content edge somewhere inside its own border; a pointer outside the container on the CROSS axis is ignored, so a drag that wandered off the strip does not keep it moving; and a pointer that stops being reported for 600ms is dropped, because a drag that leaves the window delivers no further events and the last known position would otherwise scroll forever. Scroll deltas are time-scaled, never per-frame constants, so the speed does not change with frame rate.
- **Keeping a virtualized list on its selection:** the virtualizer's own `scrollToIndex` from an effect keyed on the selected index, never a `ref` on the selected row. An inline arrow ref is a new identity every render, so React re-attaches it (and re-fires it) on every render rather than on every change of selection - and since `@tanstack/react-virtual` re-renders inside `flushSync` on every scroll-offset change, that closes a loop: a wheel tick scrolls the list, the ref re-attaches, and `scrollIntoView` snaps it back inside the same event. `FileSearchModal`'s wheel was never broken; the row ref was undoing it. `scrollIntoView` also cannot reach a row outside the rendered window, and `behavior: 'smooth'` over thousands of rows animates long enough for the next gesture to fight it. In a NON-virtualized list use `useScrollIntoView` (`src/renderer/hooks/ui/useScrollIntoView.ts`), which keys on the value. Its fourth argument picks the scroll behavior: `'smooth'` (the default) for a dropdown stepped one item at a time, `'auto'` for a list the user HOLDS an arrow key on, because key repeat outruns a smooth animation and every repeat cancels the one in flight. See [UI-PATTERNS.md → Scrolling a Virtualized List](docs/agent-guides/UI-PATTERNS.md#scrolling-a-virtualized-list-to-the-selection).
- **Sizing a virtualized row the user's font decides:** `virtualizer.measureElement` as the row's `ref`, `data-index={virtualRow.index}` on the same node, and NO inline `height`. `estimateSize` is a guess, not a height: the moment a row's real height depends on something the app does not control - the UI font, a wrapped second line, a badge only some rows carry - the guess is wrong for somebody. `FileSearchModal` stacks a file name over a directory, and under a proportional UI font those two lines did not fit its 44px constant, so the text crammed together; a fixed-pitch font wants the tighter box and must not be padded out to match, so no single number works. All three parts are required and each fails silently on its own: without `data-index` the measurement is filed against `NaN`, and an inline `height: ${virtualRow.size}px` clamps the row to the number the virtualizer already believes so measurement can never disagree with it. Read `measureElement` straight off the virtualizer (a stable instance property); wrapping it in an inline arrow re-attaches the ref every render and remeasures the whole window, the same identity trap as the entry above. `HistoryPanel` and `UnifiedHistoryTab` did this first; keep a plain `estimateSize` for a genuinely uniform one-line list (`TextPreviewFast`, `ParquetGrid`), which is cheaper. jsdom reports zero height for everything, so tests assert the wiring, not the pixels - see [UI-PATTERNS.md → Sizing a Virtualized Row](docs/agent-guides/UI-PATTERNS.md#sizing-a-virtualized-row-the-users-font-decides).
- **Adding a control to the Left Bar header:** the row is three zones - identity (wand + wordmark, `shrink-0`), indicators (`flex-1 justify-center min-w-0`, `data-testid="sidebar-header-indicators"`), menu (`shrink-0`). A status control - the trophy badge, the now-playing pill, the LIVE toggle, whatever comes next - goes in the CENTER band, never beside the wordmark; `flex-1` is what centers it between the two fixed zones. The row neither wraps nor scrolls and the sidebar goes down to 256px, so it has a declared yield order rather than whatever CSS shrinks first. The MAESTRO wordmark is drawn IN FULL or dropped entirely (`showWordmark` in `SessionList.tsx`); it must never `truncate`, because a brand rendered as "MAE..." reads as a rendering bug. The now-playing pill is the shrink target of last resort - its filename is the one thing here that can be clipped without looking broken, so the pill is `min-w-0` while every button, icon, and divider stays `shrink-0`. Reserve width for the form a control is CURRENTLY in (the pill sheds its filename when narrow, so its compact and labeled reserves are separate constants), and ask `selectNowPlayingVisible` whether the pill is on screen rather than re-deriving it. A reserve is charged only against what is still DRAWN: the LIVE label adds `headerBadgeWidth` only while `showWordmark` is true, because once the wordmark drops out the ~110px it vacated more than covers the badge, and charging anyway left a 256px sidebar showing a bare radio dot with empty space beside it. See [UI-PATTERNS.md → Left Bar Header Width Gates](docs/agent-guides/UI-PATTERNS.md#left-bar-header-width-gates).
- **Element width for JS-computed layout:** `useElementWidth(ref, enabled?)` in `src/renderer/hooks/ui/useElementWidth.ts`. Reach for it only when the value must exist in JS - an inline SVG chart needs real pixels for its viewBox, and a breakpoint that switches column counts needs a number to compare. Anything expressible in CSS stays in CSS. It returns 0 until the first measurement, so gate width-dependent children on `width > 0` rather than drawing a zero-width chart, and it no-ops without `ResizeObserver` so jsdom tests need no polyfill.
- **Usage Dashboard card tile:** `<EntityTile>` in `src/renderer/components/UsageDashboard/EntityTile.tsx` - the shared tile behind every dashboard card grid (agent grid, per-tab grid): status dot, title, badges, corner age, subtitle, labeled stat row, sparkline, plus all four border states and the staggered `card-enter` animation. A new grid shapes data into `EntityTileStat[]`; it does not re-derive the chrome. Not the same thing as the widget library's `StatCard` (one headline metric), and it stays under `UsageDashboard/` because widgets may not import from there.
- **A label that must not truncate:** `useOptionalLabelFits(rowRef)` in `src/renderer/hooks/ui/useOptionalLabelFits.ts` - returns whether an optional inline label still fits, so a caller renders it whole or omits it rather than clipping it. Reach for it when a fragment is worse than nothing: "GROUP CHA..." occupies the same row space as the full label and says less than the icon next to it. **Do NOT solve this with a container query**, which is what the Group Chats header tried first and why it clipped: the `fontSize` setting is written to `documentElement.style.fontSize`, so every rem-based size in a header scales with it while the panel's width stays in pixels, making any px threshold correct at exactly one font size; and conditional controls beside the label (a count badge, a sort button) change how much room is left, which a width threshold cannot see. The layout contract is load-bearing - the label needs `shrink-0` and the row needs `overflow-hidden`, so `scrollWidth` is the width the row wants and `clientWidth` is what it has, and overflow IS the "would have truncated" condition. The restore is the half that bites: once the label is gone the row no longer overflows, so re-showing on the same test oscillates; the hook remembers the width measured while the label was shown and returns it only at or above that, which cannot flap because that width demonstrably fit. Returns `true` before the first measurement and in jsdom, so a label renders by default. Distinct from `useElementWidth`, which reports a number for JS-computed layout and makes no decision.
- **Usage Dashboard metric tile:** `<MetricCard>` in `src/renderer/components/UsageDashboard/MetricCard.tsx` - icon, uppercase caption, one headline figure, optional sub-line, with the `role="group"` + `aria-label` that lets a test assert on a tile by its label rather than by DOM position. The Auto Run, Resilience, and Wizard sections all render rows of these; the identical component had been pasted into the first two before it lived here. Pass PRE-FORMATTED strings (`formatNumber`, `formatDurationHuman`) - it does no math. Distinct from `<EntityTile>`, which is the richer card for an agent or group in a grid, and from the widget library's `StatCard`, which lives outside `UsageDashboard/` and cannot import from it.
- **Recording wizard usage:** `beginWizardRun` / `countWizardExchange` / `recordWizardDocuments` / `finishWizardRun` (and `recordCompletedWizardRun` for a run only observed at its end) in `src/renderer/services/wizardStats.ts`, landing in `wizard_runs`. Both wizard surfaces ride it - the inline `/wizard` command keyed by AI TAB id, so parallel wizards stay separate, and the onboarding wizard as one row at launch. The design point is that **every milestone re-flushes the whole row under one stable id** rather than writing once at the end: a wizard's payoff (documents written) and its close are separated by however long the user reads the result, and plenty of runs are never closed at all, so an end-only write drops whole runs including their documents. `endedAt` therefore means "last activity", not "closed" - do NOT redefine it as a close timestamp, or "time spent in the wizard" silently becomes zero for every unfinished run. Every write is fire-and-forget and the bridge itself is null-checked: analytics must degrade to no analytics, never to a broken wizard. Do NOT add a second counter in `settingsStore.onboardingStats` - that blob is lifetime-cumulative with no timestamps, so it can never answer a dashboard time range.
- **Naming an agent from its folder (vs naming the project):** `projectNameFromPath(directoryPath)` and `defaultAgentNameForPath(directoryPath, takenNames)` in `src/renderer/components/Wizard/shared/projectIdentity.ts`. Two different things that the onboarding wizard used to derive from ONE typed string: the name in "Name your agent" was also handed to the discovery prompt as `{{PROJECT_NAME}}`, so an agent called "Scout" made the assistant greet the project as Scout, and anyone who typed their project name there got it as a Left Bar label (issue #1225). The PROJECT is always the folder - never a typed value, never the agent name. The AGENT NAME is a label the user may type and the wizard fills in from the folder when they do not, deduplicated against existing agent names because `validateNewSession` rejects a duplicate name outright and a wizard-supplied collision would be a dead end the user did not create. `defaultAgentNameForPath` returns `''` (not a name) when the path yields no basename; callers treat that as "no default available".
- **Fixed-pitch font for shell text:** `resolveFixedPitchFontFamily(fontFamily, fontSize?)` in `src/renderer/utils/fixedPitchFont.ts`, or `useFixedPitchFont(fontFamily)` in `src/renderer/hooks/ui/useFixedPitchFont.ts` from a component. Maestro has ONE `fontFamily` setting shared with the app chrome, so a user who picks a proportional UI font would otherwise get proportional terminals, command-mode composers, and shell-output cards - and shell text is a grid, so `ls -l` stops lining up and box drawing stops joining. The check is a MEASUREMENT, not a name test: CSS cannot be asked whether a face is fixed-pitch, so `isFixedPitchStack()` compares the advance of `W` against `i` (in Avenir Next, the font this was found with, they are 1025 and 296). Appending a monospace fallback is not enough on its own - a proportional font that RESOLVES never reaches the fallback - so a stack that measures proportional is replaced outright with the shared `MONO_FALLBACK_STACK`. The FALLBACK half is not this module's: it calls `withMonoFallback` from `src/shared/fontStack.ts`, and a local duplicate of that chain (`ensureMonospaceFallback`, `FIXED_PITCH_FALLBACK_STACK`) did ship here once and was folded back in - do not reintroduce one. Do NOT reach for Tailwind's `font-mono` on a surface that shows shell text: it is a different stack from the terminal's, so the same command renders in two faces between the proposal card and the transcript. It lives in `utils/` rather than `XTerminal.tsx` so the chat-side surfaces can use it without importing xterm.
- **Saving the user's own font setup:** `captureTypographySnapshot(values, savedAt?)`, `parseTypographySnapshot(raw)`, `typographySnapshotPatch(snapshot)`, `typographySnapshotMatches(snapshot, values)` in `src/shared/typographySnapshot.ts`; the store half is `saveTypographySnapshot()` / `restoreTypographySnapshot()` in `src/renderer/stores/settingsStore.ts`, behind the single `typographySnapshot` setting. Derive the keys from `TYPOGRAPHY_SURFACE_LIST` in `src/shared/typography.ts`, never from a hand-written list. Zoom is deliberately not part of a snapshot.
- **Rendering raw terminal output (ANSI):** `useAnsiConverter(theme)` in `src/renderer/hooks/ui/useAnsiConverter.ts` for the theme-aware `ansi-to-html` converter, then `getCachedAnsiHtml(text, theme.id, converter)` in `src/renderer/utils/textProcessing.ts` to convert, sanitize (DOMPurify), and cache. The 16 ANSI slots map onto the ACTIVE theme rather than the xterm palette, so do NOT hand-roll another `new Convert({...})` - a second palette drifts the first time a theme adds a color. Collapse carriage returns with `processCarriageReturns()` BEFORE converting, or a progress line arrives as a screen of dead rows, and run any regex over the output through `stripAnsiCodes()` first. Both of those live in `src/shared/stringUtils.ts` (the renderer module re-exports `processCarriageReturns` for convenience), so the main process can reach them without pulling DOMPurify into a process with no DOM - `plainGitErrorText()` in `src/main/ipc/handlers/git.ts` uses them to turn a colored `--progress` failure into text a toast can show. The renderer only owns half of this: **nothing Maestro spawns is a TTY**, so the producing side must force color too (`-c color.ui=always` for git, `FORCE_COLOR=1` / `CLICOLOR_FORCE=1` for whatever its hooks run) or the console renders a perfect wall of gray.
- **Any CLI verb that can move the Maestro view:** `resolveBackgroundFlag()` (CLI side) / `readBackgroundField()` (handler side) in `src/shared/focusPlacement.ts`. **Focus belongs to the human operator**: an agent may create a surface, it may not decide the human should be looking at it. `background: true` means the active AGENT does not change AND the active TAB inside any agent does not change - the surface is still created and addressable, so "created but invisible" is a different bug. **The flag is ADDITIVE: only one verb's default changed.** `open-file`, `open-terminal`, `open-browser`, `tab new`, `dispatch` (the plain `send_command` path, which selects the target agent "for visual feedback"), `create-agent`, `create-worktree` and `switch-mode` all still focus when the flag is absent; `refresh-auto-run` is background by default (CLI side only, `--focus` to switch), because its focusing default only moved the view when the target was off screen, so every unflagged refresh from a Cue script or agent turn pulled the user away; `dispatch --new-tab` is still background-by-default with `--focus`, which is also its pre-existing behaviour. `focus-agent`, `send --tab`, `open` and `open-graph` exist TO move the view and carry no flag at all - a full-window overlay whose only effect IS being looked at has no background reading. `refresh-files` accepts `--background` and IGNORES it (`ALREADY_QUIET_VERBS` / `isAlreadyQuietVerb`): it renders no notice and moves no selection, so there is nothing to suppress, but commander rejects an unknown option, and one verb that refused the flag would turn "pass `--background` unless the user asked to be taken there" from a rule into a lookup table. A verb belongs on that list only while it moves nothing; the moment it grows a notice or a selection it moves into `CLI_BACKGROUND_DEFAULTS` and the flag starts meaning something, with no call site changing. `readBackgroundField` is an OPT-IN - only a literal `true` counts. **Never write `message.background !== false` in a handler**: that reads an absent field as an opt-in and silently stops the verb from focusing, breaking every existing caller at once. Defaults are keyed by VERB, not message, because `tab new --prompt` and `dispatch --new-tab` share `new_ai_tab_with_prompt` and disagree. `--no-switch` on `open-file` is NOT a spelling of `--background`: it stays on the current agent but still activates the tab there, while `--background` changes nothing rendered anywhere (both may be passed; background wins). On the renderer, background placement is simply not spreading a focus patch - `createTab({ activate: false })`, `addTerminalTab(s, tab, { activate: false })`, `handleOpenFileTab(file, { activate: false })`. **A verb that fans out into a second message must CARRY its resolved bit, not let the second message re-resolve**: `create-worktree --background <msg>` created the agent quietly and was then yanked to it one message later, because the follow-on `send_command` re-resolved as `dispatch`, which focuses. **`--background` never costs the work, only the trip**: `refresh-auto-run --background` still re-reads the documents (for a non-active agent, doing nothing IS the refresh - the loader effect reads the folder fresh on switch), and it drops only the success flash, which confirms an action the USER took and not one an agent took. See [CLI-UI-PARITY.md → Focus belongs to the human](docs/agent-guides/CLI-UI-PARITY.md#focus-belongs-to-the-human---background----focus).
- **Making a tab the visible one:** the four `*FocusFields` helpers in `src/renderer/utils/tabFocusFields.ts` (`aiTabFocusFields`, `fileTabFocusFields`, `browserTabFocusFields`, `terminalTabFocusFields`), re-exported from `tabHelpers.ts`. The main window renders exactly ONE tab type by precedence (terminal > file > browser > ai), so "focus this tab" is never one assignment - missing a higher-ranked selection leaves the previous view on screen and the focus silently does nothing. They live in their own import-free module because both `tabHelpers` and `terminalTabHelpers` need them and those two already point at each other.
- **Record view for one table row:** `<RecordDetailModal>` in `src/renderer/components/ui/RecordDetailModal.tsx` - the field/value flip of a row that every tabular preview opens on double-click (CSV/TSV via `CsvRowDetailModal`, parquet via `ParquetViewer`). Callers pass a flat `fields` list plus their own `priority` / `resizeKey` / `testIdPrefix`; the mapping from a row to fields belongs in the caller, since a CSV row is positional strings and a parquet row is typed values. Do NOT hand-roll a second one - focus starts on the FIELD LIST rather than the filter input, because Left/Right step rows and Up/Down scroll and none of that works while a text input owns the caret (`/` moves focus to the filter).
- **Previewing a parquet file:** `<ParquetViewer>` in `src/renderer/components/ParquetViewer/`, over the main-process engine in `src/main/parquet/` and the shared filter language in `src/shared/parquet/`. Parquet is the one previewable format that never crosses IPC: `fs:readFile` short-circuits it to a marker (`shared/parquet/preview.ts`) and the viewer queries windows of rows through `window.maestro.parquet.*`. Three invariants: **filtering and sorting round-trip to the engine** (filtering the loaded page would only ever search the first few hundred rows of a hundred million); **`matchedRows` is a LOWER BOUND until `complete` is true**, rendered `1,204+`, converged by a background `countAll: true` pass that doubles as the prefetch; and **hiding a column changes the projection**, so the engine stops decoding it. The one thing that silently loses data: **filter literals compile into the CONVERTED domain, not the physical one** - hyparquet converts footer statistics while parsing, so a `TIMESTAMP(MICROS)` bound is epoch MILLISECONDS, and compiling microseconds prunes every row group in the file while looking healthy. See [REMAINING-SYSTEMS.md → Parquet Query Engine](docs/agent-guides/REMAINING-SYSTEMS.md#5-parquet-query-engine-srcmainparquet-srcsharedparquet).
- **Modal layer:** `useModalLayer()` in `src/renderer/hooks/ui/useModalLayer.ts` (do NOT use manual `registerLayer()` boilerplate)
- **Modal / find-bar ESC pill:** `<EscCloseButton>` in `src/renderer/components/ui/EscCloseButton.tsx`. Every modal, palette, and find bar needs a graphical exit - Escape alone strands users on remote desktop and tablets. Pass an `onClose` that does exactly what the Escape handler does (extract it into a shared `useCallback` rather than duplicating the body). Do NOT hand-roll the `px-2 py-0.5 rounded text-xs font-bold` pill; the inert `<div>` version used to be copy-pasted in nine places. See [UI-PATTERNS.md → Every Modal Needs a Graphical Exit](docs/agent-guides/UI-PATTERNS.md#every-modal-needs-a-graphical-exit-escclosebutton).
- **Dropdown/tooltip anchored to a header element:** `useAnchoredMenuPosition(menuRef, anchorRef)` in `src/renderer/hooks/ui/useAnchoredMenuPosition.ts`, rendered via `createPortal(..., document.body)`. Anything anchored inside the Main Panel header MUST use this. `absolute top-full` is silently clipped: the header wraps its left cluster in `overflow-hidden` boxes only as tall as the pill, which is how the git pill's hover card and the git status tooltip were both invisible for months. Bare `position: fixed` doesn't help either, because `.header-container` sets `container-type: inline-size` (implying `contain: layout`), making the header a containing block for fixed descendants. Since jsdom has no layout engine, a test asserting `toBeInTheDocument()` passes while the element is invisible - assert it is NOT a descendant of the header subtree instead.
- **Remembered textarea height:** `useResizableTextarea()` in `src/renderer/hooks/ui/useResizableTextarea.ts` - every `resize-y` textarea should keep the height the user dragged it to, across reopen and app restart. Spread its `style` last and pass `externalRef` if the component already owns a ref. See [UI-PATTERNS.md → Resizable Textareas](docs/agent-guides/UI-PATTERNS.md#resizable-textareas).
- **Auto-growing composer textarea:** `useAutosizeTextarea()` in `src/renderer/hooks/ui/useAutosizeTextarea.ts` (helpers in `src/renderer/utils/textareaSizing.ts`). Every composer that grows with its content rides it - AI composer, both wizard composers, group chat, feedback chat. Do NOT hand-roll `height = 'auto'` then `height = scrollHeight` again: the `'auto'` toggle collapses the internal scroll to the top, so a full, scrolling composer clipped the line being typed on every keystroke. Run it on the committed value, never inside `onChange` - an `onChange` resize never fires for dictation, draft restore, or template insertion, which grows the text without growing the box. Distinct from `useResizableTextarea`, which remembers a height the USER dragged.
- **Line numbers beside a `<textarea>`:** `<TextareaLineNumbers>` + `lineNumberGutterMetrics()` in `src/renderer/components/ui/TextareaLineNumbers.tsx`. The gutter is scroll-synced (translated by the textarea's `scrollTop`, written straight to the DOM so a fast scroll can't lag a frame) and wrap-aware (each logical line is measured against a hidden mirror sharing the textarea's font and wrap width, so number N sits on the FIRST visual row of line N). The Cue YAML editor is the one caller left - Auto Run's source editor moved to `<MarkdownEditor>` and rides CodeMirror's own gutter. Do NOT hand-roll another `value.split('\n').map((_, i) => <div>{i + 1}</div>)` gutter - that's what the YAML editor had, and it drifted out of alignment the moment the document scrolled or any line wrapped. Derive the textarea's `paddingLeft` from `lineNumberGutterMetrics(value).textPaddingLeft` rather than guessing a pixel value; the metrics are in `ch` and reserve two digits minimum so the editor doesn't reflow at line 10. See [UI-PATTERNS.md → Line Numbers on a `<textarea>`](docs/agent-guides/UI-PATTERNS.md#line-numbers-on-a-textarea-textarealinenumbers).
- **A view preference a user sets by clicking:** `usePersistedToggle(storageKey, defaultValue)` in `src/renderer/hooks/ui/usePersistedToggle.ts` - one boolean in localStorage, for a collapsed banner or a folded section that must survive the surface unmounting (a Right Bar tab switch, a re-render from new data) but isn't worth a Settings row. Storage failures degrade to in-memory only, so a storage-blocked renderer costs the user persistence, not their pane. Do NOT hand-roll another `useState(() => localStorage.getItem(...) === 'true')` pair - a collapse that resets on every re-render reads as the banner refusing to stay closed. `AutoRunNoticeBanner`'s `collapseKey` is the first caller. See [UI-PATTERNS.md → Collapsible Advisories](docs/agent-guides/UI-PATTERNS.md#collapsible-advisories-autorunnoticebanner-usepersistedtoggle).
- **Sizing a Document Graph node:** `calculateNodeWidth(label, previewCharLimit)` and `calculateNodeHeight(previewText, previewCharLimit)` in `src/renderer/components/DocumentGraph/mindMapLayouts.ts`. Do NOT space a layout off the `NODE_WIDTH` constant: with previews off a document draws as a filename pill about a third of a card wide and a fraction of its height, so reserving the card width claims roughly five times the real footprint. That is what made the radial rings thousands of pixels across, and it reached the force layout too, whose `forceCollide` radius read the same constant. The layout vocabulary itself (`MindMapLayoutType`, `LAYOUT_LABELS`, `MIND_MAP_LAYOUT_TYPES`, `nextMindMapLayout`, `isMindMapLayoutType`) lives in `DocumentGraph/layoutTypes.ts`, a leaf module with no d3 import so `settingsStore` and the `Session` interface can name a layout without pulling in the engine - it had been written out three times, and a layout added to the graph was silently rejected on the way to disk by a copy nobody updated.
- **A pane width the user sets by dragging:** `useResizablePanel()` in `src/renderer/hooks/ui/useResizablePanel.ts` for the drag itself, then pick the persistence by where the pane lives. Top-level chrome (Left Bar, Right Bar) is a real setting: pass `settingsKey` and back it with a `settingsStore` field. A pane INSIDE another surface (the Document Graph's preview pane, a split inside a panel) is a view preference: pair it with `usePersistedPanelWidth(storageKey, { defaultWidth, minWidth, maxWidth })` and OMIT `settingsKey`, or the hook writes the width a second time under a key nothing reads back. Keep the stored bounds and the live clamp separate - the stored bounds decide what may be written to disk, while the `maxWidth` passed to the drag folds in the container as it is right now, so a width that was legal on a maximized window can't swallow the canvas after the modal is resized down.
- **Sizing a surface to its longest label:** `widestLabelWidth(labels, { fontSizePx, chromePx?, minPx?, maxPx? })` and `estimateLabelWidth(label, options)` in `src/renderer/utils/labelWidth.ts`, for a dropdown or menu that should open wide enough to read its entries. It is an ESTIMATE from character count on purpose: a real measurement needs a live layout, so it is unavailable while computing an initial size and produces nothing in jsdom, whereas a per-character average is synchronous and testable and wrong by a few pixels - the right trade for a size the user can drag afterwards. Pass the font size rather than a px-per-character constant: three earlier inline copies each hard-coded their own (`7.5`, `7.5`, `7`) without recording the size it was tuned for, which is how a type-scale change turns into a clipped label with no error anywhere (see [DEDUP-TRACKER.md](DEDUP-TRACKER.md) finding #42). Always pass `maxPx` on a surface fed by user-supplied names - one pathological name otherwise opens a menu the width of the screen - and count ALL of the row's chrome in `chromePx`, icons and gaps included, or the estimate is right and the label still truncates.
- **A modal default sized to the screen:** `viewportModalSize({ width, height })` in `src/renderer/utils/modalSizing.ts`, passed as `defaultSize` to `useResizableModal`. Only for surfaces the user pans around INSIDE (a graph, a dashboard) - a form or dialog has a natural width and more room buys it nothing. A fixed default that reads as generous on a laptop is a postage stamp on a 5K display, and the user re-drags it on every machine. Memoize it once per mount: the hook re-clamps the live size on `resize`, and a default that moves under it fights that listener.
- **Focus after render:** `useFocusAfterRender()` in `src/renderer/hooks/utils/useFocusAfterRender.ts` (do NOT use `useEffect + setTimeout(() => ref.focus())`). Same file: `useFocusOnMount()` for a modal that must land with its input focused, and **`useFocusOnClose(ref, isOpen)` for handing the caret BACK when a transient surface closes**. The layer stack restores focus generically now (`useLayerStack` snapshots `document.activeElement` when a focus-capturing layer registers and hands it back when the layer unregisters, but ONLY if focus landed nowhere), so a modal registered through `useModalLayer` needs nothing. Reach for `useFocusOnClose` when the surface is NOT a registered layer (an inline editor, a popover) or when the caret should land somewhere OTHER than where it started - it wins, because the stack defers to anything that already claimed focus. It fires only on the true -> false edge, so it cannot steal focus from something the user clicked afterwards. Do NOT restore focus by hand in a modal's `onClose`: a surface dismissed with Escape never runs it, which is how focus ended up on `document.body` with the pane behind it looking ready while swallowing the next keystroke.
- **Event listeners:** `useEventListener()` in `src/renderer/hooks/utils/useEventListener.ts` (do NOT pair raw `addEventListener`/`removeEventListener` inside useEffect)
- **Debounce/throttle:** `useDebouncedValue()`, `useDebouncedCallback()`, `useThrottledCallback()` in `src/renderer/hooks/utils/useThrottle.ts` (filename is misleading - all three live here)
- **Identity-stable callback:** `useStableCallback()` in `src/renderer/hooks/utils/useStableCallback.ts` - one identity forever, always calling the newest implementation. Reach for it when a new callback identity is read as a STRUCTURAL change rather than a fresh value. The canonical case is `createMarkdownComponents()`: it builds a fresh map of component functions, so rebuilding it gives React a new component TYPE for every element and it unmounts and remounts the entire rendered document, discarding the reader's scroll position and restarting images and Mermaid. Any callback that closes over document content (a task-checkbox toggle, for instance) is reborn on every edit and triggers exactly that. Do NOT use it to silence an exhaustive-deps warning - it hides the callback from dependency arrays, so an effect that should re-run when its captured values change no longer will.
- **Render markdown:** `<Markdown preset="chat | document | wizard-bubble | release-notes">` from `src/renderer/components/Markdown/` (do NOT hand-roll `<ReactMarkdown>` + a per-surface `components`/plugin map). The chat preset is what `MarkdownRenderer` wraps. Shared internals: `buildMarkdownPlugins` (`Markdown/plugins.ts`), `preprocessMarkdown` (`Markdown/preprocess.ts`), leaf renderers in `Markdown/components/*`, and the document component map `createMarkdownComponents()` in `src/renderer/utils/markdownConfig.ts`. See [UI-PATTERNS.md → `<Markdown>`](docs/agent-guides/UI-PATTERNS.md).
- **Diagram content clipped at the SVG edge:** `expandSvgViewBoxToContent(svg, padding?)` in `src/renderer/utils/svgViewBox.ts`, called by `MermaidRenderer` immediately after the diagram is appended. Most mermaid types size their viewBox from laid-out content (`setupGraphViewbox` pads a `getBBox()`), but a few do not: `quadrantChart` hard-codes `viewBox="0 0 500 500"` and then draws the title centered at x=250 and point labels wherever the data puts them, so a long title or an edge-of-quadrant label is painted outside the viewport and the browser clips it - the visible damage is a chart title with both ends sliced off. The fix is NOT `overflow: visible`, which would let the diagram paint over the surrounding chat text; the viewBox is widened to the union of its declared box and the real content bounds, and the size attributes are scaled by the same factor so the diagram keeps its on-screen scale instead of shrinking to fit the wider box. Three properties to preserve if you touch it: it must run AFTER the element is in the document, because `getBBox()` needs a layout; it pads only the sides that actually overflow, because padding every edge nudges a correctly-sized diagram into looking like an overflow and rescales it; and it returns false untouched when `getBBox` is missing or throws, which is what keeps it inert in jsdom and on a detached or `display:none` SVG.
- **Clickable task checkboxes in rendered markdown:** `toggleTaskCheckboxAtLine(content, line)` in `src/renderer/utils/markdownTasks.ts`, plus `rehypeSourceLine` (`src/renderer/components/Markdown/rehypeSourceLine.ts`) and the `onTaskToggle` option on `createMarkdownComponents()`, which swaps in `<TaskCheckbox>`. react-markdown renders every GFM checkbox `disabled`, so those three pieces are what make one clickable - the file preview and the Auto Run panel both ride them. Do NOT count checkboxes in the DOM and map them onto the Nth `- [ ]` line: that drifts the moment a task marker appears inside a code fence. **Every component override in `createMarkdownComponents()` must forward its props** (destructure `node` out, spread the rest): `rehypeSourceLine` tags EVERY block, and `lineSync.domGetTopLineByAttr()` reads those tags to keep the file preview and the editor on the same line across the toggle. `React.createElement('p', null, children)` eats the attribute, which left HEADINGS as the only anchored blocks - so a document scrolled to the very top reported the first heading, and hitting Edit jumped there. See [UI-PATTERNS.md → Clickable task checkboxes](docs/agent-guides/UI-PATTERNS.md#clickable-task-checkboxes) and [Preview/edit scroll sync](docs/agent-guides/UI-PATTERNS.md#previewedit-scroll-sync-rides-the-same-data-source-line-tags).
- **Model/effort badges on a finished turn:** `<TurnSettingPills>` in `src/renderer/components/ui/TurnSettingPills.tsx`, fed from `LogEntry.turnModel` / `turnEffort`. Those are stamped at SEND time by `codifyTurnSettings()` in `src/renderer/utils/providerTabSessions.ts` - spread it into whatever tab patch marks the tab busy. Never label a turn from the live tab or agent value: settings are codified at send, so a model change made mid-stream must not relabel the response already running. **A QUEUED message freezes its settings when it is queued**, since the queue may not drain until several model changes later: every path that builds a `QueuedItem` spreads `captureQueuedTurnSettings()` into `item.turnSettings`, and both the pills (`markTabRunningQueuedItem`) and the spawn (`agentStore.processQueuedItem`) read it back through `codifyQueuedTurnSettings()`. The presence of the object is the capture flag - an undefined model INSIDE it means "the agent default was chosen", so never `?? ` it against the live value. See [UI-PATTERNS.md → Turn Attribution Pills](docs/agent-guides/UI-PATTERNS.md#turn-attribution-pills-turnsettingpills).
- **The tab a queued item is going to:** `resolveQueuedItemTabName(session, item)` in `src/renderer/utils/executionQueue.ts` for the LABEL, `resolveQueuedItemTarget()` in `src/renderer/utils/tabHelpers.ts` for the tab itself. Unlike `turnSettings`, a `QueuedItem`'s `tabName` is NOT frozen on purpose - it is a last-known fallback for a tab that is gone by the time the queue drains. Do NOT render `item.tabName` directly: an item queued into a brand-new tab snapshots `New` and keeps it forever, so after auto-naming the Execution Queue indicator grouped one tab under whichever name its FIRST item happened to carry, and the browser listed two rows for one tab under two different names. The resolver reads the live tab, then a closed-but-draining orphan, then the snapshot. See [UI-PATTERNS.md → Queued Item Tab Labels](docs/agent-guides/UI-PATTERNS.md#queued-item-tab-labels-resolvequeueditemtabname).
- **Editing the newest queued message:** `requestEditLastQueuedMessage()` in `src/renderer/services/editQueuedMessage.ts`. Two surfaces offer it (the `editLastQueuedMessage` chord and the palette's `Edit Last Queued Message`) and both route here, so they cannot drift on which item they pick, which tab they land on, or what they say when there is nothing to edit. Everything is read from the stores AT CALL TIME, never from a render snapshot - the pencil on a queued row reads live props, so a stale snapshot is the one way this can claim nothing is queued while a card sits on screen. A missing tab RANKS rather than filters (a closed tab must not turn into "nothing is queued"), only `type === 'command'` items are skipped, and the two empties are named apart: `Nothing queued to edit` vs `Only commands are queued`. See [RENDERER-SERVICES.md](docs/agent-guides/RENDERER-SERVICES.md#editqueuedmessagets---edit-the-newest-queued-message).
- **Whether a Force Send control exists at all:** `shouldOfferForceSend(eligibility)` in `src/renderer/utils/executionQueue.ts`, paired with `getForceSendEligibility()` (can it run?) and `getForceSendTitle()` (why not?). Rendering and enabling are separate questions: `canForce` decides DISABLED, this decides PRESENT. Only `needs-forced-parallel` is worth showing dimmed, because its tooltip names a setting the user can go turn on; `no-target-tab` could never work, and `target-tab-busy` resolves itself (a tab runs one turn at a time, so the item is already next in line). Both surfaces - the inline QUEUED card in `QueuedItemsList` and the Execution Queue browser's Send Now - call it, so they cannot drift on when the button appears, which they had already done once in both directions.
- **Model tier / effort level (`'low' | 'medium' | 'high'`):** `resolveTierModel()`, `resolveEffortLevel()`, `cheapTurnSettings()` in `src/shared/modelTiers.ts`; `MAESTRO:MODEL` markers via `findActiveModelHint()` in `src/shared/autorunModelHints.ts`; the join in `resolveTurnSettings()` in `src/shared/autorunTurnSettings.ts`. A marker's PLACEMENT is its scope (own line = from there down, end of a task line = that task only), the two layer per axis, and `'default'` stays a directive rather than collapsing to `undefined` so a task can opt out of a document-wide hint - do NOT normalize it away at parse time. One vocabulary maps to every provider, and it maps to ladder POSITIONS, not same-named strings - Claude's `high` is `max`, Codex's `low` is `minimal`. Do NOT pass a provider's own effort word through from a document, and do NOT add a tier table for a provider whose model IDs are discovered at runtime (codex, copilot-cli, opencode): a shipped guess rots into naming a model the user cannot run. Unmapped returns `undefined`, meaning inherit-the-agent's-value AND warn - never substitute silently, because a deep-planning task quietly running on the default model is the exact failure this replaced. Shared because the desktop and CLI Auto Run engines must resolve a document identically.
- **Auto Run markers (HITL / halt / model hint):** `scanMaestroMarkers()`, `findPendingHitlGate()`, `detectHaltMarker()`, `hasMaestroMarker()` in `src/shared/autorunMarkers.ts`, rendered by `remarkMaestroMarkers` (`src/renderer/components/Markdown/remarkMaestroMarkers.ts`) + `<MarkerPill>`. All three markers are HTML comments, so they render as NOTHING - and two of them silently block the next run (a live HITL gate pauses it, a halt marker makes Auto Run refuse to start), which presents to the user as "I pressed Run and nothing happened". `scanMaestroMarkers` is the rendering side and reports each marker's STATUS (`live` / `spent` / `invalid`), because presence is not the useful question: a gate above an unchecked task and one above a checked task are a character apart in the source and only the first stops the run. Do NOT hand-roll another marker regex - the engines and the pill must agree, or the pill sends the user to remove the wrong marker. Pills are wired ONLY into `createMarkdownComponents` (the document map); chat builds its own map, so an agent explaining the syntax in a message keeps rendering as prose rather than claiming a setting exists. One deliberate asymmetry: `detectHaltMarker` is NOT fence-aware (the engine must never miss a halt an agent mis-indented) while `scanMaestroMarkers` is (a pill on a documentation example would be a lie).
- **Auto Run steering notes (mid-run course correction):** `formatSteeringNotesBlock()`, `normalizeSteeringNoteText()`, `MAX_PENDING_STEERING_NOTES` in `src/shared/autorunSteering.ts`; pending state in `src/renderer/stores/autoRunSteeringStore.ts`; the operator-facing half (`submitSteeringNote`, `cancelSteeringNote`, `takeSteeringNotesForDispatch`) in `src/renderer/services/autoRunSteering.ts`. A run spawns a FRESH agent per task, so there is no conversation to interrupt: a message typed during a run used to queue a whole separate turn in a context the next task never saw. A steering note instead rides in front of the next task's prompt inside the `MAESTRO:CONDUCTOR-NOTES` block, which `src/prompts/autorun-default.md` (step 8) briefs the agent about so it knows the channel exists before it ever sees one. Two invariants: the block is prepended AFTER template substitution (a `{{VAR}}` the operator typed is their literal text, not a variable), and `takeNotes` reads-and-clears in one store write so a note is delivered exactly once - a note sent between the take and the spawn belongs to the NEXT task. Notes live in their own store on purpose: `batchRunStates` is rewritten wholesale by the runner's debounced flush, which would silently drop a note added mid-flush. Lifetime is the run (cleared at start and at `COMPLETE_BATCH`); nothing is persisted.
- **Fence-aware markdown scanning:** `forEachMarkdownLine()`, `UNCHECKED_TASK_REGEX`, `CHECKED_TASK_COUNT_REGEX`, `CHECKED_TASK_REGEX` in `src/shared/markdownTaskScan.ts`. Every Auto Run document scanner (task counting, HITL gates, model hints, human-step detection) rides this one walk, in `shared/` because the CLI engine cannot import from `src/renderer`. Do NOT hand-roll another line loop: any scanner that forgets the fence bookkeeping fires on a playbook that merely DOCUMENTS the marker syntax, and the copies drift on closing-fence length, tilde fences, and CRLF.
- **Thinking mode (`'off' | 'on' | 'sticky'`):** `THINKING_MODES`, `nextThinkingMode()`, `asThinkingMode()` in `src/shared/types.ts`. One cycle order serves the composer's Thinking chip and `maestro-cli tab thinking <tab-id> cycle`, so a click and a CLI cycle cannot disagree about what comes next - do NOT hand-roll another `['off', 'on', 'sticky']` ladder or an inline `indexOf(...) + 1` step. Narrow anything arriving from the CLI or the web bridge through `asThinkingMode()` rather than casting: `'off'` is a truthy string, and an unrecognized mode written straight into a tab is a permanently wrong chip rather than a rejected command.
- **Encore Feature flags and their default state:** `DEFAULT_ENCORE_FEATURES`, `resolveEncoreFeatures(raw)`, `EncoreFeatureFlags` in `src/shared/encoreFeatures.ts`. Four processes read these flags (the renderer store, `SETTINGS_METADATA`, the main-process Cue/stats gates, and `maestro-cli encore`), and each one used to carry its own copy of the defaults while treating a missing key as `false`. That is not a cosmetic duplicate: a feature that ships ON reads OFF in the main process until something persists the object, so on a fresh install the UI showed Cue running while the stats lineage writer and the telemetry gate for the same run were silently disabled. Resolve every read through `resolveEncoreFeatures()` - it merges onto the defaults and only lets a real boolean override one, so a settings file written before a flag existed keeps that flag's default instead of falling to `undefined` -> false. A capability ships off (a plugin) and its entry here flips to `true` when it graduates to an Encore Feature; do NOT re-declare the literal anywhere else.
- **Ordering the Auto Run run list from the picker:** `applySelectionOrder(documents, selectedFilenames, createEntry)`, `selectFolderFiles(prev, files)`, `deselectFolderFiles()`, `selectAllDocuments()` in `src/renderer/utils/documentSelectionOrder.ts`. The run list is an ordered program - document 1 runs, then document 2 - and the "Select Documents" picker is where that order is authored, so selection order IS run order. The picker used to emit `[...documentsAlreadyInTheList, ...newlySelected]`, which pinned anything already added to the front: a user who reopened the picker and clicked folders in the order they wanted them to run got the old order back with the new folders bolted on the end. `applySelectionOrder` rebuilds the list from the selection alone, reusing existing entries so ids, reset flags, and duplicate rows survive and every duplicate stays grouped with its original. `selectFolderFiles` deletes before re-adding so a folder's documents land as one contiguous block at the end even when one of them was already picked individually - without that, clicking a folder leaves a stray document behind at an earlier position. Do NOT rebuild the list from `allDocuments` or the document tree: both are alphabetical, and the user's click order is the only place the intended run order exists.
- **Whether an Auto Run is parked waiting on the user:** `useAutoRunErrorPaused(sessionId)` in `src/renderer/hooks/batch/useAutoRunPause.ts`. `batchRunState.isRunning` stays true through a pause, so a surface that asks "is a run active?" to decide whether to lock the document locks it during the exact pause that exists to let the user edit: an agent error the user has to fix, or a `MAESTRO:HITL` gate answered by ticking the box the marker sits above. Split the two questions - `isRunning` means the run is alive (so Stop stays offered), `errorPaused` means it is idle and asking (so the editor and the preview checkboxes open back up). Read the flag through this hook, NEVER off the `batchRunState` prop: the chain store -> `useBatchProcessor` -> `useBatchHandlers` -> `App` -> `RightPanel` -> `AutoRun` drops `errorPaused` updates through `updateBatchStateAndBroadcast`/`UPDATE_PROGRESS`, so a prop read reports `false` for a run that is plainly paused on screen. The web surface (`src/web/mobile/AutoRunInline.tsx`) has no store and reads `autoRunState.errorPaused` directly, which arrives over the wire intact.
- **What the Thinking pill lists:** `collectThinkingItems(sessions)` in `src/renderer/utils/tabHelpers.ts` builds the busy (agent, tab) pairs; `useBackgroundAutoRuns(activeSessionId)` in `src/renderer/hooks/batch/useBackgroundAutoRuns.ts` adds Auto Runs on OTHER agents. Two traps made the old inline loop wrong. A closed tab sits in `orphanedThinkingTabs` IDLE while it still owns queued items (a HELD message parks it there indefinitely), so counting every orphan showed "Thinking..." and a Stop button for a tab with no process; count orphans only while `state === 'busy'`. And Auto Run never marks a tab busy, so a run on another agent is not a thinking item at all: without the hook it had no +N badge and no dropdown row. The hook subscribes narrowly (running ids, their names, their run states) so log streaming does not re-render the pill.

If your use case does NOT match an existing utility, prefer extending the canonical file over creating a new one. If you genuinely need something new, add the full entry HERE and a one-line index entry in CLAUDE.md so the next person can find it.

The tracker at [DEDUP-TRACKER.md](DEDUP-TRACKER.md) lists all known duplication findings.
